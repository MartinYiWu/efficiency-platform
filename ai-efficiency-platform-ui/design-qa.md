# 设计验收记录

## PAGE-07 简历上传

- 参考设计稿：`产品文档/05-设计稿/AI人事助手/03-简历中心/07-简历上传.png`
- 实际路由：`/ai-assistants/hr/resumes/upload`
- 桌面（1440px）：左侧配置区与右侧导入区保持双栏；上传区、8 条文件状态、固定提示栏与设计稿一致。
- 平板（1024px）：保留双栏，文件行的文件名、状态文案按可用宽度省略，操作按钮不被挤出容器。
- 移动端（390px）：外层导航切换为抽屉入口；内容为单列，`scrollWidth <= clientWidth`，文件行改为三列折行展示。
- 已验证交互：授权撤销后“开始上传并解析”禁用；文件上传、粘贴文本、从人才库选择三种入口可切换；“查看告知同意说明”可打开弹窗。
- 规格补全：人才库选择页签增加“选择已入库候选人后，会按当前解析设置重新处理”的辅助说明；该页签在截图中未呈现详细中间态，文案仅解释既有功能规格，不新增操作。
- 规格冲突处理：视觉稿的压缩包提示为 200MB，功能规格限定为 100MB；页面展示与校验均采用功能规格的 100MB。
- 未联调项：文件上传、解析队列、OCR、去重、人才库查询与批次创建均为前端本地演示交互，待 Java 契约接入。

## PAGE-08 批量解析进度看板

- 参考设计稿：`产品文档/05-设计稿/AI人事助手/03-简历中心/08-批量解析进度看板.png`
- 实际路由：`/ai-assistants/hr/resumes/batches/20260901-001`；全部完成状态通过同一路由的 `?state=completed` 呈现，不增加设计稿之外的页面入口。
- 桌面（1440px）：进度统计、深色实时日志、明细表格和行内异常/合并提示对齐；`scrollWidth = clientWidth`，没有页面级横向滚动。
- 平板（1024px）：进度统计重排为多行，表格在明细卡内保持，不挤出页面容器。
- 移动端（390px）：外层导航切换为抽屉入口；顶部操作按钮换行，进度统计重排，日志与明细的必要横向内容限制在对应容器内；`scrollWidth <= clientWidth`。
- 已验证交互：筛选胶囊、自动滚动开关、失败重试与原因提示、疑似重复合并建议、前往低置信修正，以及“中止解析”确认弹窗；全部完成状态含“查看匹配结果”和“处理待确认项（2）”。
- 规格补全：设计稿要求“全部完成状态”替换总进度卡，但未规定独立页面入口；以前端状态参数呈现该替换状态，仅用于承载已定义状态，未增加用户可见功能入口。
- 未联调项：解析进度推送、日志流、文件重试、去重合并、解析结果与匹配计算均为前端本地演示交互，待 Java 契约接入。

## PAGE-09 低置信字段人工修正

- 参考设计稿：`产品文档/05-设计稿/AI人事助手/03-简历中心/09-低置信字段人工修正.png`
- 实际路由：`/ai-assistants/hr/resumes/candidates/wang-qiang/confirm`
- 桌面（1440px）：原简历与解析结果双栏呈现；高置信、需确认、未识别与当前定位四种标记可辨，底部操作条保持可见。
- 平板（1024px）：原简历与解析结果顺序堆叠，`scrollWidth = clientWidth`；未产生页面级横向滚动。
- 移动端（390px）：导航改抽屉入口，操作按钮单列、两栏内容依次排列，`scrollWidth <= clientWidth`。
- 已验证交互：缩放按钮、原简历定位、团队规模确认、角色选择、同义词处理、保存提示、重新解析提示，以及“跳过，按现有结果继续”的二次确认弹窗。主按钮在核对计数未完成时保持禁用，符合设计稿的保护状态。
- 规格补全：设计稿给出“已核对 0/6”与四项低置信字段，但未给出全部六项的逐项展开态；页面将可完成的核对数统一为 4，并以四个真实低置信字段的唯一确认状态驱动主操作，避免重复点击即可绕过保护。
- 未联调项：原简历渲染、解析字段、修正审计、重新解析和重新匹配均为前端本地演示交互，待 Java 契约接入。

## PAGE-10 候选人档案详情

- 参考设计稿：`产品文档/05-设计稿/AI人事助手/03-简历中心/10-候选人档案详情.png`
- 实际路由：`/ai-assistants/hr/talent/zhang-wei`
- 桌面（1440px）：候选人信息顶栏、280px 概览栏、档案标签页和结构化经历双栏信息对齐；`scrollWidth = clientWidth`。
- 平板（1024px）：概览卡重排为两列，主体档案保持单列可读，`scrollWidth = clientWidth`。
- 移动端（390px）：概览卡、主体档案和操作条堆叠；技能表的宽内容限制在区块内部，`scrollWidth = clientWidth`。
- 已验证交互：下载简历、下载档案、修正档案跳转、岗位匹配入口、标签页切换和查看完整联系方式二次确认。联系方式默认脱敏，二次确认后提示查看留痕与 30 秒恢复脱敏规则。
- 未联调项：档案、版本、技能证据、匹配记录、导出任务和联系方式审计均为前端本地演示交互，待 Java 契约接入。

## PAGE-11 至 PAGE-15 匹配与人工复核

- 实际路由：`/ai-assistants/hr/matching`、`/ai-assistants/hr/matching/zhang-wei`、`/ai-assistants/hr/matching/filtered`；PAGE-14、PAGE-15 依照设计稿作为上述流程中的模态窗口呈现。
- 已验证：匹配分档固定使用绿/蓝/橙/灰；证据可定位；被过滤简历独立展示；权重合计校验与影响预览；人工放行原因必填与二次确认。没有淘汰、拒绝、候选人外发或录用决策入口。
- 响应式：匹配表格的横向内容仅在自身容器内滚动，1024px 与 390px 主页面不产生横向溢出。
- 未联调项：匹配计算、权重重算、人工放行审计与导出均为前端演示交互，待 Java 契约接入。

## PAGE-16 至 PAGE-18 面试准备

- 实际路由：`/ai-assistants/hr/interviews/generate`、`/ai-assistants/hr/interviews/questions`、`/ai-assistants/hr/interviews/guide`。
- 已验证：生成配置覆盖轮次、时长、角色、维度、题量、批量状态与来源预览；题集支持展开、编辑、删除确认和手动新增；导出页锁定 AI 分数、分档、维度分及匹配理由，联系方式导出会显示审计提示。
- 规格补全：窄屏下生成配置保持设计稿的弹窗语义，改为弹窗内部纵向滚动，避免裁切或页面级横向滚动。
- 未联调项：题目生成、导出文件和审计写入均为前端演示交互，待 Java 契约接入。

## PAGE-19 至 PAGE-21 与 PAGE-01

- 实际路由：`/ai-assistants/hr/talent`、`/ai-assistants/hr/settings/synonyms`、`/ai-assistants/hr/settings/audit`、`/ai-assistants/hr/workbench`。
- 已验证：人才库语义搜索、列表/卡片切换、保留期提示与档案入口；词条筛选、新增弹窗、歧义别名和待处理技能；审计筛选、敏感操作详情、脱敏 IP 与不可修改提示；工作台的五类待办、快捷入口、概览、岗位进展及首次使用引导。
- 未联调项：人才检索、词库维护、操作审计、导出与工作台数据均为前端演示交互，待 Java 契约接入。

## 2026-09-02 全量复核（PAGE-01、08–21）

- 浏览器以 1440×900、1024×768、390×844 巡检 13 个独立路由（PAGE-01、08–13、16–21），共 39 次：每次均命中对应页面标题，且 `scrollWidth <= clientWidth`，未发现页面级横向滚动。
- PAGE-14 在匹配列表内打开“调整权重并重算”弹窗，确认权重合计、影响预览与历史结果保留提示均存在；PAGE-15 在被过滤简历页打开“人工放行硬性条件”弹窗，确认不可放行锁定项、原因输入与放行后预览均存在。两者在 390px 下也无页面级横向滚动。
- 关键高复杂画面已复核：PAGE-11 的五档结果表与右侧概览在 1440px 保持对齐；PAGE-17 的手机端顶部操作改为两行，导出按钮完整可达；没有出现导航与内容割裂或文字被横向裁切。
- 自动化回归补强：PAGE-14 新增权重合计/影响预览/确认重算按钮测试；PAGE-15 新增原因必填、不可放行锁定及确认按钮状态测试。

## 2026-09-02 版式治理复核（A 方案）

- 共享容器：`PageContainer` 增加标题操作区与粘性语义，页面内容最大宽度调整为 1600px 内自适应；不增加业务入口或路由。
- PAGE-01：待办与岗位进展采用有明确主次比例的双主列，1199px 以下收为单列；岗位表的横向滚动仅保留在表格容器内。
- PAGE-11、PAGE-19、PAGE-21：原有静态分页文案替换为受 `page`、`pageSize` 和数据切片驱动的 Ant Design 分页。当前为前端演示数据，分页总数以已加载样本为准；后端接入后应由契约返回真实总数和分页数据。
- PAGE-17：三栏工作区在实际内容宽度不足时先将设置栏下沉，保证题目编辑主区宽度；窄屏显示层级摘要，题目卡、筛选条和设置卡不重叠。标题区主操作保持首屏可达，底部固定条仍保留设计稿要求的保存/导出语义。
- PAGE-19：修复长筛选栏导致结果卡被网格拉伸的空白问题，结果区改为内容高度；分页紧跟结果，不再以大面积空白对齐左栏。
- 本轮以 1440×900、1024×900、390×900 对 PAGE-01、08–13、16–21 的 13 个独立路由复核，共 39 次。所有记录均为 `scrollWidth = clientWidth`；局部横向内容仍限制在表格、日志或预览容器中。已人工截图复核 PAGE-01、08–13、16–19。
- 本记录只证明当前前端演示的布局、交互状态和响应式边界；解析、匹配、分页总数、审计和导出数据仍待后端契约联调。
