# AI 内容运营助手专业化交付体验进度账本

## Task 1：V1/V2 实体 Schema 与展示模型

- 状态：已实施，待独立复核。
- 范围：`src/entities/operation-deliverable/`、`src/features/operation-chat/types.ts` 与本账本；未修改页面、Reducer、网络层、依赖或环境配置。
- DTO 对照：V1 保持 `deliverable-set/1` 与 `deliverable/1` 原字段；V2 对照 Agent `deliverable-set/2`，包含五种 `deliverable_kind`、`content.kind` 判别联合、来源、质量提醒、后续动作及采集 provenance。
- 安全边界：对象使用严格 Schema；V2 显式按 `contract_version` 分派，不根据字段形状猜版本；未知字段、未知交付类型、顶层与内容类型不一致、非 HTTPS 的 V2 来源均失败关闭。`intent_patch` 仅接受 JSON 值，映射阶段不读取或推断其业务含义。
- V1 兼容：旧 reducer 暂时经 `types.ts` 的 V1 兼容适配层消费实体解析结果；完整双版本 `parseDeliverableSet` 从实体层公开出口提供，Task 2 扩展 reducer 状态后移除该过渡层。
- 红灯：`pnpm test -- src/entities/operation-deliverable/schemas.test.ts` 在实体模块尚不存在时失败，错误为无法解析 `./mappers`。
- 绿灯：`pnpm test -- src/entities/operation-deliverable/schemas.test.ts src/features/operation-chat/messageReducer.test.ts`，15 tests passed；`pnpm typecheck` 退出码 0；范围内 ESLint 与 Prettier 检查均退出码 0。
- 文件快照基线：`C:\Users\Administrator\AppData\Local\Temp\codex-sdd-efficiency-platform\ui-task-1-baseline`。

### Task 1 R2：独立复核问题修复，待复审

- 修复范围：`src/entities/operation-deliverable/{model,schemas,mappers,index}.ts` 与 `schemas.test.ts`；未改动 DTO 字段、Reducer、页面、网络层、依赖或环境配置。
- 根因与处理：V2 URL 原先只校验 `https:` 协议，现同时要求 hostname 且禁止 username/password；`intent_patch` 现限制为最多 50 个顶层键，与 Agent 契约对齐。V1 DTO 继续原样保留 `CitationV1.url`，但 Legacy ViewModel 的 `CitationV1ViewModel.href` 仅在无凭据 HTTPS URL 时非空，供后续渲染层只使用该字段创建链接。
- TDD 红灯：`pnpm test -- src/entities/operation-deliverable/schemas.test.ts` 为 3 failed / 6 total，分别证明 credential URL、51 键 `intent_patch`、V1 非安全 URL 无安全 href。
- 绿灯：`pnpm test -- src/entities/operation-deliverable/schemas.test.ts src/features/operation-chat/messageReducer.test.ts` 为 18 passed；`pnpm typecheck`、`pnpm lint`、范围内 `pnpm exec prettier --check ...` 均退出码 0。
- 全局格式检查：`pnpm format` 退出码 1；输出列出既有计划/设计文档、`pnpm-lock.yaml`、`playwright-report-agent/` 及其他不在本任务白名单内的文件，未越界格式化。Task 1 范围内 Prettier 已通过。

### Task 1 R3：页面过渡层安全渲染修复，待独立复审

- 修复范围：`src/pages/operations-assistant/chat/OperationsChatPage.{tsx,test.tsx}`；未改动 DTO 契约、Reducer、网络层、依赖或环境配置。
- 根因与处理：页面仍直接将 V1 `citation.url` 写入 `a.href`，绕过了实体层的 Legacy ViewModel 安全链接判定。页面现通过 `mapDeliverableSet` 获取 Legacy ViewModel，仅当 `citation.href` 非空时渲染外链；不安全或带凭据 URL 保留为纯文本，兼容原始来源可读展示。
- TDD 红灯：新增页面测试后，`pnpm vitest run src/pages/operations-assistant/chat/OperationsChatPage.test.tsx` 为 1 failed / 3 total，证明 `javascript:` URL 被错误渲染成外链。
- 绿灯：页面、实体和 Reducer 聚焦测试为 21 passed；`pnpm typecheck` 与 `pnpm lint` 均退出码 0；两个页面文件的 Prettier 检查退出码 0。
- 全局格式检查：`pnpm format` 退出码 1，仅剩既有计划/设计文档、`pnpm-lock.yaml`、`playwright-report-agent/` 和未在本修复范围内的现有前端文件；未越界格式化。
- 文件快照基线：`C:\Users\Administrator\AppData\Local\Temp\codex-sdd-efficiency-platform\ui-task-1-r3-baseline`。

### Task 1 独立复核结论

- Task 1：complete。R3 独立复核通过，无 Critical/Important/Minor。
- 当前页面仅使用实体层映射出的可选 `citation.href` 创建外链；`javascript:` 与含 username/password 的 HTTPS URL 均保持可读文本且不可点击，无凭据 HTTPS 来源仍可访问。
- 新鲜页面、实体与 reducer 聚焦验证共 21 项通过；全局格式检查遗留 23 个不在本任务范围内的既有文件，未越界改写。

## Task 2：双版本集合、真实阶段状态与可访问进度

- 状态：已实施，待独立复核。
- 范围：`src/features/operation-chat/{messageReducer,types,sseClient}.{ts,test.ts}`、`OperationProgress.{tsx,test.tsx}`、`index.ts`，以及仅为新必填状态补齐 mock 的 `src/pages/operations-assistant/chat/OperationsChatPage.test.tsx`；未改页面行为、网络 API、实体 DTO、依赖或环境配置。
- 阶段事实边界：只接收 Agent 契约的五个公开阶段，并由 Agent 实际发送的 `phase_started`、`research_started`、`research_completed`、`content_generation_started`、`content_generation_completed`、`quality_checked` 事件驱动。未知阶段（包括对象原型伪键）不改变展示状态；客户端不从文本、时间或计时器推断进度。
- 计数与生命周期：仅保存非负安全整数且 `completed <= target` 的成对计数；新提交清空阶段和交付，失败、取消、终态冻结已收到的真实阶段，等待澄清与终态拒绝迟到阶段事件。V1/V2 均经实体层 `parseDeliverableSet()` 校验，非法交付不进入集合或消息。
- 可访问性：`OperationProgress` 使用单一 `role="status"` 与 `aria-live="polite"`，只在成对有效计数存在时显示计数，不创建定时器、百分比或虚拟步骤。
- 红灯：新增测试后，`pnpm test -- src/features/operation-chat/messageReducer.test.ts src/features/operation-chat/OperationProgress.test.tsx src/features/operation-chat/sseClient.test.ts` 出现 8 个预期失败：五个真实 Agent 阶段事件被 SSE 拒绝、Reducer 缺失 V2/阶段状态、进度组件不存在。随后新增的原型伪阶段测试单独红灯，证明 `toString` 曾被错误接纳。
- 绿灯：`pnpm test -- src/features/operation-chat/messageReducer.test.ts src/features/operation-chat/OperationProgress.test.tsx src/features/operation-chat/useOperationChat.test.tsx src/features/operation-chat/sseClient.test.ts` 为 31 passed；`pnpm typecheck`、`pnpm lint`、范围内 `pnpm exec prettier --check ...` 均退出码 0。
- 文件快照基线：`C:\Users\Administrator\AppData\Local\Temp\codex-sdd-efficiency-platform\ui-task-2-baseline`。

### Task 2 R2/R3：归属与本地生命周期复核修复

- R2 将 SSE 接收收紧为当前已绑定 Run，V2 内部 `run_id` 必须匹配事件与当前 Run；reset、终态与取消期间的迟到 phase/deliverable 均不再复活页面，取消失败恢复 streaming 后才可继续显示真实阶段。
- R3 继续收紧本地 `run_created`、`local_failed`、`cancel_requested`、`cancelled`、`cancel_failed` 生命周期 action：它们仅在当前 request/run 的合法转换上生效；提交前取消的安全路径仍保留。
- 最终独立复核通过，无 Critical/Important/Minor。新鲜验证：Reducer、Progress、Hook、SSE 四文件 34 项通过，`pnpm typecheck` 通过。全局格式检查仍仅受既有范围外文件影响，未越界改写。

## Task 3：热点榜与来源依据组件

- 状态：已实施，待独立复核。
- 范围：仅新增 `src/widgets/operation-delivery/{RankedDigestView,SourceEvidencePanel}.tsx`、`OperationDelivery.module.css`、`RankedDigestView.test.tsx`；未修改会话状态、实体 DTO、页面、依赖或环境配置。
- 可访问结构：热点榜使用具名语义 `ol` 与连续的 `li` 编号，条目提供标题、时间、摘要、运营价值、来源数量、可信度与核验状态的文字表达；复制按钮只把 Agent 已提供的 `copyText` 交给注入回调，不重新拼接内容。
- 来源与安全：`Collapse` 默认收起来源；展开后显示采集时间窗、排序口径、候选/合并/保留/淘汰、已核验与冲突计数，以及来源标题、日期与核验状态。只有无凭据 HTTPS URL 可成为带 `noopener noreferrer` 的外链；不安全 URL 仍保留可读来源文字，且不以裸 URL 作为主文本。
- 响应式：文档容器最大阅读宽度为 920px；920px 以内缩小内边距；390px 以内将文档、元数据和来源统计切为单列/可换行布局。
- TDD 红灯：`pnpm test -- src/widgets/operation-delivery/RankedDigestView.test.tsx` 在组件尚不存在时因无法解析 `./RankedDigestView` 失败。随后新增连续编号断言时，输入第二项为 rank 9 的缺口被暴露为错误显示 `9.`，据此改为按已核验集合顺序连续编号。
- 绿灯：`pnpm test -- src/widgets/operation-delivery/RankedDigestView.test.tsx` 为 3 passed；`pnpm typecheck`、范围内 `pnpm lint -- src/widgets/operation-delivery` 与四个白名单文件的 Prettier 检查均退出码 0。
- 文件快照说明：四个任务文件在实施开始时均不存在，独立复核应将其作为新增文件检查；本任务没有执行 Git 操作。

### Task 3 R2：热点榜编号视觉回归修复，待独立复审

- 根因：语义 `ol` 的浏览器默认 `decimal` 标记与标题中由组件明确输出的连续编号叠加，视觉上会显示两套序号。
- 处理：保留 `ol`/`li` 的列表结构与标题连续编号，在作用域 `.rankedList` 上显式设置 `list-style: none`（同时设置等价长属性，确保测试环境和浏览器均无标记）。
- TDD：新增计算样式与两项标题编号的可观察断言后，聚焦测试预期红灯，证实 `listStyleType` 为 `decimal`；最小 CSS 修复后聚焦测试 4 passed。
- 新鲜验证：`pnpm typecheck`、`pnpm lint -- src/widgets/operation-delivery/RankedDigestView.test.tsx` 与两个修改文件的 Prettier 检查均退出码 0。仅将 CSS 文件传给 ESLint 会得到其不支持 CSS 配置的忽略警告，故未将其作为 ESLint 验收项。

## Task 4：平台内容、计划、诊断、复盘与后续动作

- 状态：已实施，待独立复核。
- 范围：新增 `PlatformContentView.tsx`、`StructuredOperationViews.tsx`、`OperationNextActions.tsx`、feature 出口与两组组件测试；仅扩展 `OperationDelivery.module.css` 的文档式/结构化响应式样式。未改 DTO、SSE、页面、网络层、依赖或环境配置。
- Markdown 与复制边界：平台正文固定 `skipHtml`；仅无 username/password 的 HTTPS Markdown 链接以 `target="_blank" rel="noopener noreferrer"` 打开，其余仅渲染可读文本。复制按钮只将 Agent 已提供的 `copyText` 交给回调，不从 Markdown 或 HTML 重组内容。
- 结构化语义：行动计划按目标、受众、阶段、行动与指标展示；诊断复制排序后按 high/medium/low 显示证据和建议，并保留数据缺口；复盘连续显示目标、结果、差距、原因、下一步五个二级区块。
- 后续动作边界：运行时仍以六种 Agent 契约动作白名单过滤；`show_sources` 仅调用本地展开回调，其余仅将 `label` 作为下一轮可见消息交给 `onSubmit`；任何 `intent_patch` 都不读取、不发送、不渲染。
- TDD 红灯：`pnpm test -- src/widgets/operation-delivery/StructuredOperationViews.test.tsx src/features/operation-next-action/OperationNextActions.test.tsx` 在组件尚不存在时为 2 个预期失败套件，错误均为无法解析待创建组件模块。
- 绿灯：计划规定三文件测试为 12 passed；加上热点榜回归为 16 passed；`pnpm typecheck`、`pnpm lint` 均通过，范围内 Prettier 写入后无格式变化。
- 全局格式检查：`pnpm format` 仍因任务范围外既有计划/设计文档、`playwright-report-agent/` 产物、`pnpm-lock.yaml` 与已有流式组件文件列为未格式化而退出码 1；本任务未越界改写。

### Task 3 独立复核结论

- Task 3：complete。R2 独立复核通过，无 Critical/Important/Minor。
- 新鲜验证：热点榜聚焦测试 4 项、`pnpm typecheck`、范围内 lint 与 Prettier 检查均通过；语义列表仅显示一套连续编号。

### Task 4 独立复核结论

- Task 4：complete。独立复核通过，无 Critical/Important/Minor。
- 新鲜验证：结构化视图与后续动作 12 项目标测试、类型检查、范围内 lint 和 Prettier 检查均通过；未修改会话协议、实体 DTO 或 HR 页面行为。

## Task 5：统一交付 Widget 与运营页面集成

- 状态：complete。初次独立复核发现三项 Important（来源动作未真实接线、`provenance=null` 被渲染为已核验 0 条、复制能力未进入页面）和一项 Minor（页面测试跨层使用 feature 私有入口）；R2 已按 TDD 修复并由独立代理复审通过，无 Critical/Important/Minor。
- 最终行为：`show_sources` 仅展开本地来源面板且不发送会话消息；缺少采集 provenance 时明确显示“核验统计未提供”；热点榜和平台正文复制严格使用 Agent 原始 `copyText`，并提供成功状态/失败告警；测试和页面均经 feature 公共出口引用。
- 新鲜验证：R2 相关测试 20/20、operation-chat 41/41、`pnpm typecheck`、全量 lint 与范围 Prettier 均通过；HR CSS 未改。完整 build、覆盖率和浏览器 E2E 留待 Task 6/7。

## Task 5：统一交付 Widget 与运营页面接入

- 状态：已实施，待独立复核。
- 范围：新增 `src/widgets/operation-delivery/{OperationDelivery.tsx,index.ts}` 与 `src/pages/operations-assistant/chat/OperationsChatPage.module.css`；修改运营聊天页、其组件测试和既有交付 CSS 模块。未修改 `HrChatPage.module.css`，因此 HR 页面行为与视觉不受本任务影响。
- 交付分派：`OperationDelivery` 仅消费实体层已校验的 V1/V2 `DeliverableSetDto`，统一映射后将 V2 五类交付物分派给热点榜、平台正文、行动计划、诊断或复盘组件；V1 使用文档式兼容视图，不伪造 V2 质量字段或热点条目。
- 安全与可见动作：V1 正文采用 `skipHtml`，其 Markdown 外链仅在无凭据 HTTPS 时创建；V1 引用也只使用实体层映射出的可选安全 href。全局/单交付 warnings 和降级状态使用 `Alert`。V2 后续动作继续只将既有 label 传回当前 `sendMessage`，不读取或发送 `intent_patch`。
- 页面边界：页面移除旧的 inline generic `Card` 交付渲染，改为公开 Widget 出口；真实 `OperationProgress` 仅在当前任务 busy 且存在 Agent 阶段时出现。普通流式消息、取消、错误和澄清区域保持原逻辑。
- 响应式：运营页独立 CSS Module 覆盖宽屏基础、`max-width: 1279px` 中屏和 `max-width: 767px` 窄屏；文档交付保持 920px 最大阅读宽度，短消息继续采用气泡，结构化交付不叠加大阴影。
- TDD 红灯：在旧页面中新增 V2 热点榜与后续动作两项可观察页面测试后，`pnpm test -- src/pages/operations-assistant/chat/OperationsChatPage.test.tsx` 为 2 failed / 5 total，分别因没有 V2 内容和没有动作按钮失败。
- 绿灯：`pnpm test -- src/pages/operations-assistant/chat/OperationsChatPage.test.tsx src/widgets/operation-delivery src/features/operation-chat` 为 54 passed；`pnpm typecheck`、`pnpm lint` 与任务文件 Prettier 检查均退出码 0。
- 文件快照：本实现子任务未创建独立的 Task 5 事前快照；独立复核需以父任务保留的任务基线或工作区差异包核验新增/修改范围。本任务未执行 Git 操作。

### Task 5 R2：独立复核问题修复，待复审

- 修复范围：`src/widgets/operation-delivery/{OperationDelivery,SourceEvidencePanel,RankedDigestView,PlatformContentView}.tsx`、`src/features/operation-chat/index.ts`、`src/pages/operations-assistant/chat/OperationsChatPage.test.tsx` 与本账本；未修改 SSE、Reducer、API、实体 DTO、依赖、环境配置或 HR 页面。
- 根因与处理：统一 Widget 未向后续动作提供 `onShowSources`，也未向交付视图注入复制回调；来源组件把缺失 provenance 的 `verified_source_count` 用 `0` 兜底；页面测试直接导入 feature 私有文件。现由 Widget 受控管理来源展开，`show_sources` 仅设置本地展开状态；复制只在用户点击后把 Agent `copyText` 原样交给 `navigator.clipboard.writeText`，成功显示 `role="status"`，失败显示 `role="alert"` 且页面保持可用；页面测试和所需 `OperationChatState` 类型统一经 `features/operation-chat` 公共出口消费。
- 安全边界：`show_sources` 集成测试用会抛错的 `intent_patch` getter 证明渲染和点击路径没有读取该字段，同时断言 `sendMessage` 零调用；原始 HTML/Markdown 安全策略与仅无凭据 HTTPS 外链逻辑未改动。
- TDD 红灯：新增真实 `OperationsChatPage` + `OperationDelivery` 集成覆盖后，`pnpm test -- src/pages/operations-assistant/chat/OperationsChatPage.test.tsx` 为 4 failed / 9 total，分别证明来源动作被禁用、null provenance 误报“已核验 0 条”、复制按钮不可见、复制失败无可访问反馈。
- 绿灯与范围回归：页面聚焦测试 9 passed；页面 + Widget + 后续动作测试 20 passed；`src/features/operation-chat` 回归 41 passed，覆盖普通流式、取消、错误、澄清及既有生命周期。
- 静态门禁：`pnpm typecheck`、`pnpm lint` 均退出码 0；六个本次代码白名单文件的 `pnpm exec prettier --check ...` 退出码 0。首次 lint 曾准确拦截 effect 内同步 setState，随后改为可选受控展开状态并重跑通过。
- 项目边界：本目录为非 Git 项目，本轮未运行任何 Git 命令。

## Task 6：V1/V2 Mock E2E、响应式与键盘验收

- 状态：complete；Mock 浏览器验收与相关前端回归已通过，真实 Agent/真实来源质量仍属于 Task 7，不在本任务内宣称通过。
- 最终范围：修改 `e2e/operations-chat-acceptance-matrix.spec.ts`，新增 `e2e/operations-chat-professional-delivery.spec.ts`，并仅为 E2E 暴露的焦点接续缺口修改 `src/pages/operations-assistant/chat/OperationsChatPage.tsx`；同步本账本。`playwright.agent.config.ts` 现有项目只匹配 live-fake-Agent，用不到 testMatch 扩展，因此未修改，也未增加第二个 server。
- V1/V2 矩阵：保留 8 条 V1 普通内容、三平台、活动、热点来源、澄清、降级、取消和重连行为；新增严格有效 `deliverableSetV2()` fixture helper，并覆盖热点榜、平台内容、行动计划、诊断、复盘 5 类 V2，共 13 条矩阵用例。V1 来源仍默认折叠，E2E 通过真实展开操作验证安全来源和原警告，未伪造 V2 语义。
- 专业交付：9 条新 E2E 覆盖热点榜 1–9、来源默认折叠和 `aria-expanded`、部分降级、非法 V2 失败关闭、后续动作沿用同一 conversation id、进度 `aria-live="polite"`、1440x1024/1024x768/390x844 三视口无水平溢出及输入/来源/动作可达，并用 Tab 后的 Enter/Space 验证来源和动作键盘触发。
- 最小页面修复：后续动作按钮在新一轮提交时会被移除；页面现在只为该路径记录焦点接续意图，在 busy 控件渲染后的下一动画帧把焦点交给真实“停止生成”按钮。普通输入框发送不强制移动焦点，未添加隐藏测试 DOM。
- TDD 红灯：首次运行新专业交付文件为 2 failed / 9 total；其中 phase-only SSE 立即 EOF 属于 fixture 生命周期问题，改为保持事件流开放后收敛为 1 failed / 9 total，稳定证明 Space 触发后焦点落到非交互区域。焦点修复后该单用例通过。首次联合运行为 21 passed / 1 failed（22 total），暴露 V1 来源 E2E 缺少展开步骤；一次默认展开尝试导致相关 Vitest 19 passed / 1 failed，随后撤销并按现有公开交互补齐 E2E 展开步骤。
- 最终绿灯：`pnpm exec playwright test e2e/operations-chat-acceptance-matrix.spec.ts e2e/operations-chat-professional-delivery.spec.ts --reporter="list,html"` 为 22/22 passed；`pnpm test -- src/pages/operations-assistant/chat/OperationsChatPage.test.tsx src/widgets/operation-delivery src/features/operation-next-action` 为 20/20 passed；`pnpm typecheck`、四个任务代码文件的范围 ESLint 与范围 Prettier 均退出码 0。
- Playwright 报告：`D:\软件目录\efficiency-platform\ai-efficiency-platform-ui\playwright-report\index.html`，本轮生成时间 2026-09-17 19:01:22，大小 534255 字节。失败过程的 Playwright error context 位于本地 `test-results/`，最终成功报告为上述 HTML。
- 项目边界：本任务全程未执行 Git 命令；未修改依赖、环境变量、Agent 服务、后端、SSE/Reducer/DTO 契约或 HR 页面。

### Task 6 R2：后续动作快速终态焦点修复，待独立复审

- 修复范围：仅修改 `e2e/operations-chat-professional-delivery.spec.ts`、`src/pages/operations-assistant/chat/OperationsChatPage.tsx` 与本账本；未修改 Agent、SSE、Reducer、实体/DTO 契约、依赖、环境配置或 `show_sources` 本地展开行为。
- 根因：原实现进入 busy 后在动画帧执行前就清除了焦点接续标记；若第二轮 SSE 在该帧前立即成功或失败，React 清理会取消动画帧，原后续动作按钮又已卸载，焦点最终落到 `body`。
- 最小处理：后续动作独占的焦点状态只有在真实目标获得焦点后才清除。busy 时聚焦可见的“停止生成”；若成功终态先到，聚焦最新交付中的首个可用按钮；失败、澄清或无可操作交付时回退到真实输入框。普通输入提交不设置该焦点意图；若用户在停止按钮聚焦后主动移到其他控件，终态不会抢回焦点。未添加隐藏元素或测试专用 DOM。
- TDD 红灯：新增“Space 触发后续动作且第二轮立即成功”和“第二轮立即失败”两条 E2E 后，运行 `pnpm exec playwright test e2e/operations-chat-professional-delivery.spec.ts --grep "第二轮立即" --reporter=list` 为 2 failed / 2 total；两条均在 5 秒内持续观察到 `document.activeElement` 为 `BODY`。
- 绿灯：同一聚焦命令为 2/2 passed；成功路径焦点进入新公众号交付的“复制全文”按钮，失败路径焦点回到“输入消息”。联合 `pnpm exec playwright test e2e/operations-chat-acceptance-matrix.spec.ts e2e/operations-chat-professional-delivery.spec.ts --reporter=list` 为 24/24 passed，继续覆盖普通提交、取消/停止、重连、busy 时停止按钮焦点，以及 1440x1024、1024x768、390x844 三视口。
- 相关回归：`pnpm test -- src/pages/operations-assistant/chat/OperationsChatPage.test.tsx src/widgets/operation-delivery src/features/operation-next-action` 为 20/20 passed；`pnpm typecheck` 与全量 `pnpm lint` 均退出码 0。任务文件 Prettier 在格式写入后重新检查。
- 项目边界：本轮未执行任何 Git 命令；真实 Agent/真实来源质量仍属于 Task 7，本轮不宣称其验收通过。

## Task 7：自动化预检与真实 Agent 运行环境

- 状态：自动化预检已执行；真实 Agent 冒烟 E2E 已通过；十轮真实浏览器会话、三视口截图和最终验收报告仍待主验收流程完成，本节不宣称 Task 7 整体验收通过。
- 范围：修改 `e2e/operations-chat-real-agent.spec.ts`，将原 `page.route` 响应替换为真实 Agent HTTP/SSE 请求；修改 `playwright.config.ts`，把默认 Playwright UI 从 Agent CORS 不允许的 `4173` 对齐为项目约定的 `5190`；追加本账本。未创建最终验收报告或十轮证据 JSON，未修改 Agent 源码、密钥、业务组件、DTO、Reducer 或 SSE 客户端。
- TDD RED 1：PowerShell 静态守卫检查 `e2e/operations-chat-real-agent.spec.ts` 不得包含 `page.route(`，初次退出码 1，错误为 `REAL_AGENT_E2E_INTERCEPTS_NETWORK`。移除请求拦截后同一守卫退出码 0，输出 `REAL_AGENT_E2E_NETWORK_INTERCEPTION_FREE`。
- 真实链路 RED 2：`pnpm exec playwright test e2e/operations-chat-real-agent.spec.ts --reporter=list` 首次为 1 failed，60 秒超时。页面显示 `Failed to fetch`，Agent 访问日志显示来自 Playwright `4173` 的 `OPTIONS /v1/conversations/.../messages` 返回 400；源码与 API 测试证明 Agent 只允许 `http://127.0.0.1:5190` 和 `http://localhost:5190`。最小修复为把默认 Playwright `baseURL`/Vite 端口改成 `5190`，没有放宽 Agent CORS。
- 真实链路 RED 3：对齐 5190 后，同一用例在 1.2 秒内准确收到正式服务响应，但测试错误期待 202，实际和 Agent 路由声明均为 201 Created。仅把 E2E 状态断言校正为 201，没有修改服务契约或弱化正文、SSE、网络路径、控制台与告警断言。
- GREEN：`pnpm exec playwright test e2e/operations-chat-real-agent.spec.ts --reporter=list` 为 1/1 passed；启动持久 UI 后复跑仍为 1/1 passed。用例提交 `你是谁`，验证真实 `POST /v1/conversations/{conversation_id}/messages` 返回 `conversation/1`、真实 `GET /v1/runs/{run_id}/events` 返回 `text/event-stream`、页面出现 Agent 身份回复、无 `role=alert` 且无浏览器 console warning/error。该身份快路径不调用外部模型，因此只证明正式 Agent 进程、Conversation API、SSE 与页面消费链路，不代表外部模型或真实来源质量通过。

### Task 7 前端完整门禁

按计划顺序执行：

1. `pnpm format`：退出码 1。首次与白名单格式化后复跑均列出同一批 22 个历史范围外文件，包含两份既有计划/设计文档、`playwright-report-agent/` 生成物、`pnpm-lock.yaml`、`StreamingAssistantContent.tsx` 及其测试。本轮仅执行 `pnpm exec prettier --write e2e/operations-chat-real-agent.spec.ts`，结果 unchanged；未批量改写范围外文件。
2. `pnpm lint`：退出码 0。
3. `pnpm typecheck`：退出码 0。
4. `pnpm test:coverage`：退出码 0；`coverage/coverage-final.json`、`clover.xml` 与 HTML 报告已于本轮更新。命令回传只显示 Vitest 启动与 coverage enabled，未据此虚构测试数量。
5. `pnpm build`：退出码 0，3562 modules transformed，构建 11.71 秒；保留 Zod 注释位置与主 chunk 1740.99 kB 的 Rollup 警告，不将警告描述为失败。
6. `pnpm test:e2e`：退出码 0，33/33 passed，27.0 秒；包含 V1/V2 Mock 行为矩阵、三视口与键盘用例，以及不使用网络拦截的真实 Agent HTTP/SSE 冒烟用例。

完整门禁当前结论为 `PARTIAL`：除全局格式基线仍失败外，其余五项退出码为 0；不得表述为六项全绿。

### Task 7 端口身份与可重复入口

- 启动前只读预检：`5190` 与 `8080` 均无 listener；未按固定 PID 停止任何既有服务。
- Agent 启动命令：在 `D:\软件目录\efficiency-platform\efficiency-platform-agent` 执行 `uv run python -m efficiency_platform_agent --env-file D:\软件目录\efficiency-platform\efficiency-platform-agent\.env --host 127.0.0.1 --port 8080`。`.env` 仅核验键名和 SET/EMPTY 状态，未回显任何值。8080 listener 的完整命令行指向同一项目正式模块；`GET http://127.0.0.1:8080/health/live` 返回 200 和 `{"status":"ok","mode":"s2_fake_in_memory"}`。正式组合根日志记录 `deepseek_chat` 与 `free_public_research` enabled，但本轮身份冒烟未调用外部模型，不能据此判断十轮质量。
- UI 启动命令：在 `D:\软件目录\efficiency-platform\ai-efficiency-platform-ui` 执行 `node D:\软件目录\efficiency-platform\ai-efficiency-platform-ui\node_modules\vite\bin\vite.js --host 127.0.0.1 --port 5190`。5190 listener 的完整命令行指向本 UI 的 Vite 入口；`GET http://127.0.0.1:5190/ai-assistants/operations/chat` 返回 200、`text/html`。
- 运行入口：页面 `http://127.0.0.1:5190/ai-assistants/operations/chat`，Agent 健康地址 `http://127.0.0.1:8080/health/live`。两者保持单实例监听，供后续内置浏览器十轮真实会话复用。
- 启动纠偏：首次通过 pnpm PowerShell 包装启动时参数中多出字面量 `--`，Vite 因其他项目占用 5173 而错误落到 5174。核验完整命令和项目路径后，仅停止本轮误启的 UI 进程树；其他项目的 5173/5174 listener 未触碰。随后改用明确的本项目 Vite JS 入口启动 5190，未遗留重复实例。
- 阻塞与边界：自动化唯一未闭合项是全局 `pnpm format` 的 22 个历史范围外文件；真实来源、外部模型十轮内容质量、受控来源不足/冲突、三视口截图与最终验收材料尚未在本节执行。
