# Java–Agent contract

The future HTTP contract between `ai-efficiency-platform` and `efficiency-platform-agent` is owned jointly here and in the Agent repository. Java-facing contract anchors currently live in `aiep-platform-ai` under `api` and `agent`.
