# 意图与研究 V2 持久化变更说明

- `change_id`: `20260916_001_意图与研究V2`
- 类型：Schema、约束、索引
- 目标对象：`agent_runtime` 内 Intent revision、Research action/decision/artifact、Budget lease 与独立 Checkpoint 表
- 数据规模：隔离验收库为空；生产规模尚未评估，禁止直接用于共享/生产库
- 非目标：不访问 Java 表、不安装扩展、不保存全文到 RunEvent、不自动启动清理调度
- 租户边界：所有业务唯一键包含 `tenant_id`；共享来源预算仅 ledger 聚合键共享，租户 usage 仍独立
- 保留策略：HTML 最长 7 天、证据摘录 30 天、脱敏元数据 90 天；来源或租户更短时取较短值
- UNKNOWN_OUTCOME：远程调用已派发但本地提交未知时保守计费，默认禁止盲目重试
- 事务/超时：单事务，`lock_timeout=5s`、`statement_timeout=60s`
- 回滚：只将 `intent_research_v2_writes` 关闭，不 DROP 表、不删除 V2 事实，旧读保持可用
- 授权：2026-09-17 用户“全部都授权”，仅限 `efficiency_agent_x01_*` 隔离数据库；共享/生产环境必须重新授权

