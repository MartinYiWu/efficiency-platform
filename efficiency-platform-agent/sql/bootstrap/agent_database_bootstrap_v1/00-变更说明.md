# Agent 数据库 bootstrap v1 变更说明

- `change_id`: `agent_database_bootstrap_v1`
- 类型：Agent 独立 Schema 与权威迁移账本首次初始化
- 目标：仅在隔离 PostgreSQL 创建 `agent_runtime` 与 `schema_changes`
- 非目标：不创建业务表、不访问 Java Schema、不安装扩展
- 事务：单事务，事务级 advisory lock `7887332933888385649`
- 回滚：仅当 Schema 内只有 bootstrap 对象且尚未承载普通变更时，才允许使用 `04-rollback.sql`
- 授权：2026-09-17 用户“全部都授权”，仅限本项目隔离验收环境

