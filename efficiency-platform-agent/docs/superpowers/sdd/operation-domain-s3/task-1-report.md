# S3 Task 1：运营任务与请求契约

状态：已完成（离线契约）。

- 目标文件：`contracts/errors.py`、`contracts/task.py`、Task1 单元测试。
- GREEN：`uv run python -m unittest tests.unit.operation.test_task -q`，6 tests passed。
- 约束：身份链、稳定 ID、关键条件完整性、可修改假设和领域错误均已校验。
- 未实现：自然语言意图识别、模型调用、业务执行和外部持久化。
