# S4 受控多 Agent 协作进度账本

> 项目永久不使用 Git。本账本以任务级测试、文件级证据和实际命令输出追踪；未有新鲜证据的任务不得标记完成。

## 执行状态

| 任务 | 状态 | 证据 | 未验证边界 |
|---|---|---|---|
| Task 1：依赖核验与核心契约 | 已完成 | 核心契约 6 项单元测试通过；架构门禁通过 | 未连接真实 S2/S3 外部实现 |
| Task 2：受限任务 DAG | 已完成（离线） | [Task 2 报告](task-2-report.md)；8 项单元测试、mypy、compileall 通过 | 真实 S2/S3、Registry、调度、LangGraph 和外部系统未验证 |
| Task 3：父子预算账本 | 已完成（离线） | [Task 3 报告](task-3-report.md)；6 项单元测试、Ruff、mypy 通过 | 尚未接入 Scheduler、真实可信 Usage、Graph Runtime 和持久化恢复 |
| Task 4：Specialist 选择与派发 | 已完成（离线） | [Task 4 报告](task-4-report.md)；4 项聚焦测试、Ruff、mypy、compileall 通过 | 尚未接入 Scheduler、真实 Specialist、真实 S2 Usage 或外部系统 |
| Task 5：有界并行调度 | 已完成（离线） | [Task 5 报告](task-5-report.md)；8 项状态/调度测试、Ruff、format、mypy 通过；取消唤醒、`asyncio.timeout()` 和候选切换已回归 | 未接入真实 AgentFactory、GraphRuntime、S2 Usage、LangGraph 和 EventStore |
| Task 6：部分失败与聚合 | 已完成（离线） | [Task 6 报告](task-6-report.md)；6 项聚合/修订测试、Ruff、mypy、compileall 通过 | 尚未接入 Scheduler、Graph、真实 Specialist、可信 Usage 和外部系统 |
| Task 7：Supervisor Graph Builder | 已完成（离线） | [Task 7 报告](task-7-report.md)；契约、Fake 三端口闭环、Builder/Factory 构建、取消状态保留、等待恢复与状态顺序回归通过；S2 Checkpoint 适配已补充同步/异步存储兼容测试 | 真实 LangGraph 跨进程恢复、取消唤醒和可信 Usage 尚未验证 |
| Task 8：端到端验收与架构守卫 | 已完成（离线） | [Task 8 报告](task-8-report.md)；架构守卫、代表性验收、状态不变量与事件顺序回归通过，Ruff、mypy、format、compileall 通过 | 真实等待恢复/取消唤醒、LangGraph 持久检查点和生产端到端验收尚未执行 |
| Task 9：交付文档与审查包 | 已完成（离线） | [S4阶段交付报告](S4阶段交付报告.md)、[文件级差异与需求追踪矩阵](文件级差异与需求追踪矩阵.md)；全量回归 296 passed、243 子测试、mypy 120 文件、compileall 通过 | 真实恢复/取消、生产基础设施和真实端到端行为仍未验证 |

## Task 1 证据

- 新增 `src/efficiency_platform_agent/core/multi_agent.py`，只包含冻结的框架中立状态、上限、任务图、派发包装、结果和可信预算消耗契约。
- 新增 `tests/unit/core/test_multi_agent_contracts.py`，覆盖不可变性、平台硬上限、非法放大、空图、不可变序列、S1 `SupervisorTask` 包装和结果校验。
- RED/GREEN 及文件级核验以本任务测试和依赖对照表为准；未修改 S1～S3 公共接口。

## 当前全局边界

- Task 1～6 已完成框架中立的离线能力；Task 7 已形成可执行的 GraphRuntime/S3 Fake 闭环，但真实等待恢复/取消和可信 Usage 尚未验证；Task 8 已完成离线架构守卫与代表性验收；Task 9 已完成阶段报告、文件级差异和需求追踪矩阵，后续任务必须继续遵守单一 Graph Runtime、S2 生命周期和 S3 契约边界。
- 当前尚未完成等待恢复/取消顺序、可信 Usage 和持久检查点的阶段级验收，因此不能宣称 S4 整体完成。
- 所有验证均为离线验证；真实模型、网络、数据库、Redis、COS 和生产运行行为未验证。

## Task 4 证据

- 新增 `src/efficiency_platform_agent/agents/operation/supervisor/selection.py`：按 Specialist 元数据执行任务类型、能力、输入/输出 Schema、权限、工具和最小执行预算六项硬过滤，并按额外能力/权限/工具、费用和 Agent ID 稳定排序；`ContextProjector` 只输出请求的 Profile 引用和来源范围。
- 新增 `src/efficiency_platform_agent/agents/operation/supervisor/dispatch.py`：按预算账本和 Agent 上限构造 S1 `SupervisorTask`/`TaskDispatch`；结果解码器接受版本化信封、校验负用量，并对 S3 `EvidencePack`、非空 `DeliverableBundle` 和 `OperationQualityReport` 做离线无损解码。
- 新增 `tests/unit/agents/operation/supervisor/test_selection.py`、`test_dispatch.py` 与 `tests/support/supervisor_fakes.py`；未导入业务 Specialist、Provider、网络或外部存储。

## Task 6 证据

- 新增 `src/efficiency_platform_agent/agents/operation/supervisor/aggregation.py`：仅按图顺序收集 TaskOutcome 和已解码 S3 对象，明确区分 COMPLETE/PARTIAL/FAILED/WAITING_INPUT/CANCELLED，并保留冲突。
- 新增 `tests/unit/agents/operation/supervisor/test_aggregation.py`；覆盖可选/必需失败、依赖跳过、证据重复保留和修订/任务数门禁。

## Task 7 证据

- 新增运营策略载荷、恢复值和 Supervisor 事件契约；策略载荷只接受 `operation_request`，恢复值包含请求、计划修订及 fence 校验，事件模型禁止额外字段并可转换为 S2 `strategy_event`。
- 新增框架中立 Supervisor 节点、稳定路由、S2 Runtime 执行委托、唯一 LangGraph Builder 与 MULTI_AGENT 组合根注册工厂；未修改 S2/S3 公共接口。
- 仅完成 Fake/离线结构验证；真实 checkpoint 恢复、取消唤醒、可信 Usage 和外部依赖尚未验证。
