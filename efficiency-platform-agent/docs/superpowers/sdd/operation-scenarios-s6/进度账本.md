# S6 运营场景包执行账本

> 项目永久不使用 Git。本账本以任务报告、文件级清单、实际测试输出和未验证边界作为可追溯证据；不记录密钥、真实用户正文、真实账号或外部网页原文。

## 执行状态

| 任务 | 状态 | 当前证据 | 未验证边界 |
|---|---|---|---|
| Task 1：前置门禁与测试支架 | 已完成（复核） | 场景生产依赖、Sample 隔离和禁止第二状态的契约测试 | 历史 RED 未单独留存；未做真实运行时验收 |
| Task 2：场景入站与 Supervisor 窄契约 | 已完成（复核） | `ScenarioSubmission`、`ScenarioSupervisorRequest`、`ScenarioExecutionResult` 及 Protocol；场景契约测试通过 | 仅 Fake 端口；未接入真实 Supervisor |
| Task 3：Manifest Registry 与八个 Pack | 已完成 | [task-3-report.md](task-3-report.md)；4 项 Registry 测试通过 | 未执行真实 Specialist 或外部数据源 |
| Task 4：24 个固定样本与 Fake | 已完成 | [task-4-report.md](task-4-report.md)；24 个样本和转换隔离测试通过 | 样本不代表真实业务数据和模型质量 |
| Task 5：提交服务与状态边界 | 已完成（复核） | `ScenarioPackService.run(submission)` 唯一入口；四类场景服务回归通过 | 未连接真实 Supervisor、Provider、存储或网络 |
| Task 6：九项确定性质量门禁 | 已完成 | [task-6-report.md](task-6-report.md)；多平台独立指纹门禁通过 | 未验证语义质量、事实正确性和真实平台规则 |
| Task 7：行业、多平台、品牌、IP Pack | 已完成 | [task-7-report.md](task-7-report.md)；12 项完整/等待/部分回归通过 | 未验证真实研究、内容质量和平台效果 |
| Task 8：活动、日历、增长、复盘 Pack | 已完成 | [task-8-report.md](task-8-report.md)；9 项服务级回归通过 | 未验证真实数据、归因和外部触达 |
| Task 9：跨 Pack 与架构隔离证据 | 已完成（复核） | `test_scenario_supervisor_contract.py`、能力目录契约和场景全量回归 | 文件级历史快照未统一归档；真实集成未执行 |

## 最新离线验证

在 `D:\efficiency-platform\efficiency-platform-agent` 执行：

```text
$env:PYTHONPATH='src'; uv run pytest tests/unit/operation/scenarios tests/contract/operation tests/contract/agents/operation -q
44 passed

$env:PYTHONPATH='src'; uv run python -m unittest tests.unit.operation.test_scenarios tests.unit.operation.scenarios.test_service tests.unit.operation.scenarios.test_registry tests.unit.operation.scenarios.test_samples tests.unit.operation.scenarios.test_quality tests.unit.operation.scenarios.test_industry_digest tests.unit.operation.scenarios.test_multi_platform_content tests.unit.operation.scenarios.test_brand_and_ip_operation tests.unit.operation.scenarios.test_campaign_and_calendar tests.unit.operation.scenarios.test_growth_and_review -q
结果以当前全量 unittest 证据为准：249 tests 全部通过
```

项目级最新门禁：pytest `497 passed`、unittest `249 tests`、Ruff、mypy（169 个源文件）和 compileall 均通过。上述证据均为离线 Fake/InMemory，不代表真实模型、网络、数据库、Redis、COS 或生产可用。

## 生产边界

- S6 只声明场景、校验入站、选择 S3 Profile、编译 S3 Plan、调用 S4 窄端口并执行确定性结构门禁。
- S6 不拥有第二运行时、第二状态枚举、具体 Agent 路由、Provider SDK、网络客户端、文件解析、账号管理或平台发布能力。
- 不主动发布内容、不发送消息、不接入付费商业数据源、不执行 Text2SQL；真实外部动作统一留给 S7 授权 Gate。

## 下一阶段可依赖接口

`ScenarioSubmission`、`ScenarioSupervisorRequest`、`ScenarioExecutionResult`、`ScenarioPackService.run()`、`InMemoryScenarioPackRegistry`、`build_s6_manifests()` 和九项 `DeterministicScenarioQualityGate` 规则。调用方不得绕过 S3 Profile 选择和 S3 Plan 编译直接提交 Plan、TaskGraph、状态或结果。
