# S7 真实集成与验收进度账本

| 任务 | 状态 | 证据 |
|---|---|---|
| Task 1：阶段对齐与默认关闭控制面 | 已完成（离线，控制面已补强） | 依赖对照表、授权模板、预检脚本、`task-1-report.md`、19 项配置/授权契约测试；已补充动作前上下文匹配、子上限校验及 DOCUMENT_PIPELINE/END_TO_END 延迟键校验 |
| Task 2：真实集成依赖锁定 | 已完成（离线） | `task-2-report.md`、`tests/admission/test_s7_dependencies.py`；24 个批准包已声明并锁定 |
| Task 3：DeepSeek OpenAI 兼容 Provider | 已完成（离线） | `task-3-report.md`、`providers/llm/deepseek.py`、6 项 Provider 契约测试 |
| Task 4：DeepSeek 服务端 web_search Provider | 已完成（离线） | `task-4-report.md`、`capabilities/research/deepseek_web_search.py`、Provider Stub 测试 |
| Task 5：PostgreSQL/pgvector 隔离与 SQL 变更包 | 已完成（离线） | `task-5-report.md`、Provider/Repository/Checkpoint/检索适配器、SQL 五文件、8 项契约/治理测试 |
| Task 6：Redis Run Event 与 Taskiq 单 Worker 离线适配 | 已完成（离线） | `providers/cache/redis.py`、`tasks/broker.py`、`tasks/acceptance_probe.py`、契约/集成测试、`task-6-report.md` |
| Task 7：COS Artifact 隔离对象生命周期 | 已完成（离线） | `task-7-report.md`、5 项 COS Provider 契约测试 |
| Task 8：Fixture 生成与文档/分析链路 | 已完成（离线骨架，Docling 本地样本已验证） | `scripts/build_s7_fixtures.py`、`tests/fixtures/s7/manifest.json`、`task-8-report.md`、预检/摄取/OCR/上传 API/Polars 端口/治理测试；上传路由已支持显式注入应用组合根；默认 PDF 路径失败关闭，只有显式注入本地管线才初始化；PaddleOCR 空模型目录失败关闭；失败摄取不会登记 upload_id，可重试 |

| Task 9：离线回归与确定性质量集 | 已完成（离线） | `task-9-report.md`、十条固定 JSONL、评估测试、需求到证据追踪矩阵 |
| Task 10：代表性场景与部分失败验收 | 已完成（离线接线） | `scripts/s7_verify.py`、`scripts/s7_cleanup.py`、`tests/acceptance/s7` 四组场景（10项）及 `task-10-report.md`；授权文件、清理清单、损坏 JSON 和目录目标错误均失败关闭 |
| Task 11：人工验收、生产边界与停用回滚文档 | 已完成（离线） | `生产边界与停用手册.md`、`S7阶段交付报告.md`、`test_s7_acceptance_documents.py`（3项） |

2026-09-04 新鲜复核：`uv run pytest tests/contract/providers/test_redis_provider_contract.py tests/integration/s7/test_redis_taskiq.py -q`，9 项通过；该结果仍为协议 Fake/离线验证，不代表真实 Redis 或 Worker 已验收。
2026-09-04 新鲜场景复核：`uv run pytest tests/acceptance/s7 tests/evaluation tests/integration/s7 -m "not real_external" -q`，20 项通过；覆盖离线场景、质量集、上传/解析链路，未触发真实外部 I/O。
2026-09-04 G0 新鲜复核：`uv run pytest -m "not real_external" -q` 为 497 passed、2 warnings、254 subtests；unittest 为 249 tests OK；Ruff format/check、mypy（169 个源码文件）、compileall 和 `uv lock --check` 均通过。
2026-09-07 G0 依赖升级后复核：`uv run pytest -m "not real_external" -q` 为 497 passed、2 warnings、254 subtests；unittest 为 249 tests OK；Ruff format/check、mypy（169 个源码文件）、compileall、`uv lock --check` 和 `uv audit --frozen` 均通过，`pypdf_security=clear`。
2026-09-07 G0 增加 G4/G5 关闭态探针守卫后的复核：`uv run pytest -m "not real_external" -q` 为 499 passed、2 warnings、254 subtests；unittest 为 249 tests OK；Ruff format/check、mypy（169 个源码文件及验收脚本）、compileall、`uv lock --check` 和 `uv audit --frozen` 均通过。
2026-09-07 G0 增加 G2 web_search 受控入口、G3 PostgreSQL 真实入口、G7 零网络预检、外部清理目标校验和关闭态守卫后的复核：`uv run pytest -m "not real_external" -q` 为 519 passed、2 warnings、254 subtests；unittest 为 249 tests OK；新增真实探测/写入入口默认关闭测试、G7 预检治理测试及外部清理目标测试均通过，Ruff format 检查 329 个文件、Ruff、mypy、compileall、`uv lock --check` 和 `uv audit --frozen` 均通过。G7 预检对非法子 Gate 和损坏授权记录均失败关闭。
2026-09-04 依赖越界扫描：禁止包集合未出现在 `pyproject.toml` 直接依赖中，准入测试 3 项通过；`uv.lock` 中出现的 `beautifulsoup4`、`openpyxl`、`langchain-core` 为 Docling/LangGraph 的传递依赖，未被 Agent 源码直接导入或声明，已按“直接依赖禁止、传递依赖说明”分类。
2026-09-04 G1 预检：`uv run python scripts/s7_preflight.py --gate deepseek_chat` 显示 `enabled=False`；当时 `授权记录.json` 无授权记录，因此未创建客户端、未发起网络请求。
2026-09-07 G1 NETWORK_PROBE：在负责人授权、单次调用和无写入限制下，`s7_verify.py --gate deepseek_chat --execute-real` 成功调用模型列表接口；脱敏结果为 `SUCCEEDED`，`network_calls=1`、`model_calls=0`，证据写入 `evidence/g1-deepseek-network-probe.json`。
2026-09-07 G1 CHARGED_ACCEPTANCE：在第二份独立计费授权、`deepseek-v4-flash`、单次模型调用和零正文持久化限制下，最小结构化请求成功；脱敏结果为 `SUCCEEDED`，`network_calls=1`、`model_calls=1`、`input_tokens=114`、`output_tokens=70`，证据写入 `evidence/g1-deepseek-chat-acceptance.json`。Provider 未返回价格，故 `cost_observed=false`，不对实际费用作推断。
2026-09-07 G1 收尾：真实验收完成后已将 `.env` 中 `S7_GATE_DEEPSEEK_CHAT` 恢复为 `false`；后续再次运行必须重新取得新的动作级授权。
2026-09-07 其余 Gate 配置复核：在不修改 `.env` 开关且不输出任何配置值的前提下，将 Gate 状态仅在内存中临时视为启用；当时 G3 PostgreSQL/pgvector 和 G7 共同缺少 `AGENT_RUNTIME_DATABASE_URL`，其余 Gate 配置键齐全。G2 搜索仍受“付费商业数据源不启用”约束，未发起搜索调用。
2026-09-07 G4 NETWORK_PROBE：在独立授权、最多 2 次网络动作和零写入限制下，两个 Redis 目标 PING 均成功；脱敏结果为 `SUCCEEDED`，证据写入 `evidence/g4-redis-network-probe.json`。尚未启动 Worker、写入事件或执行清理回读。
2026-09-07 G4 WRITE_ACCEPTANCE：真实 Taskiq/事件验收在能力预检阶段安全失败关闭，结果为 `redis_streams_unsupported`；非敏感能力复核显示目标 Redis 为 `4.0.8`，不支持 Redis Streams 的 `XADD`，因此未进入 Worker、未写入事件，清理回读为零，证据为 `evidence/g4-redis-taskiq-acceptance.json`。当前 Redis 版本升级或切换到支持 Streams 的目标前，G4 写入验收不能通过。
2026-09-07 最新只读复核：`AGENT_TASK_BROKER_URL` 与 `AGENT_RUN_EVENT_REDIS_URL` 仍指向同一 Redis `10.13.3.227:6001`，服务版本仍为 `4.0.8`、模式为 standalone；未发现可替代的 Redis 5+ 目标，未执行任何写入。
2026-09-07 G5 NETWORK_PROBE：首次使用存储桶访问域名作为 SDK 服务端点导致 Bucket HEAD 返回 `CosClientError`，已保留失败证据 `evidence/g5-cos-network-probe-failed.json`；修正为使用官方区域服务端点后，在新的独立授权下 Bucket HEAD 成功，`cos_objects=0`，成功证据为 `evidence/g5-cos-network-probe.json`。全程未创建对象。
2026-09-07 G5 WRITE_ACCEPTANCE：在独立写入授权、单对象/1 MiB/8 次网络动作上限下，完成 COS 上传、HEAD、读取、签名、删除和精确前缀回读；`network_calls=7`、`cos_objects=1`、`bytes_written=15`、`cleanup_ok=true`，证据为 `evidence/g5-cos-artifact-acceptance.json`。COS 账单未由 SDK 返回，`cost_observed=false`。
2026-09-07 G3 NETWORK_PROBE/WRITE_ACCEPTANCE：针对 Agent 自有 `jjcy_ai_vector` 完成 PostgreSQL 16.11 与 pgvector 只读预检及独立 Schema 写入授权验收。使用临时运行配置将同一物理库映射为 Runtime 逻辑 DSN，未修改项目 `.env`；验证 Run/Event/Usage、乐观版本、跨连接 Checkpoint、1024 维 vector、原生 FTS、租户过滤均通过，5 条合成记录随后精确回滚并回读为空。证据为 `evidence/g3-postgres-network-probe.json` 与 `evidence/g3-postgres-pgvector-acceptance.json`。
2026-09-07 G7 零网络预检能力补齐：`scripts/s7_preflight.py --gate end_to_end` 现在会核对选定子 Gate、配置键、授权有效期、脱敏成功证据、清理清单和预算上限；当前运行结果为 `BLOCKED`，未创建任何外部客户端，原因包含独立 Runtime DSN 缺失、G7 动作授权缺失/过期和清理清单不完整。
2026-09-07 G2 受控入口补齐：`s7_verify.py` 新增 `probe_deepseek_web_search` 与 `run_deepseek_web_search_acceptance`，复用 S5 ResearchProvider 契约，只保存来源计数、严格记录 `source_urls_opened=0`，并受独立网络/计费授权和搜索预算约束；因当前禁止付费商业数据源且 Gate 保持关闭，未发起真实搜索。
2026-09-07 清理边界补强：`s7_cleanup.py` 新增 PostgreSQL Schema、Redis 前缀和 COS 前缀的精确格式校验，拒绝通配符、`FLUSHDB`、`FLUSHALL` 和全局目标；仍保持默认不执行外部清理。

## 当前未闭合的 13 项计划步骤

以下步骤保持未勾选是有意的，均需要动作级授权、真实外部资源或人工样本，不属于可在当前离线范围内代替完成的实现：

- G1 DeepSeek：已完成网络探测授权/调用与计费验收授权/调用；保留流式子能力未选取，因此不纳入本次 Gate；
- G2 服务端搜索：计费/网络授权与真实调用 2 项；
- G3 PostgreSQL：本次只使用 Agent 自有向量库临时映射完成，原始 `.env` 仍缺少独立 Runtime DSN；后续若要求独立物理 Runtime 库需重新执行 3 项；
- G4 Redis/Taskiq：真实 Worker 验证、清理回读 2 项；
- G5 COS：已完成网络探测和真实写入生命周期；
- G6 用户文件：受控文件人工 Gate 1 项；
- G7 端到端：动作级授权、真实场景、无条件清理 3 项；
- 人工样本：四类人工验收 1 项；
- 真实验收判定：Redis/Taskiq、完整文档/OCR链路、人工质量和真实运行清理 4 项；PostgreSQL 条件已由本次合成 Schema 验收覆盖。

除 G1 的 1 次网络探测/1 次最小模型调用、G3 的独立 Schema 合成验收、G4 的 2 次 Redis PING、G5 的 Bucket HEAD 及单对象生命周期外，其他 Gate 仍保持关闭，未执行搜索、Redis 写入或用户文件 I/O；G3 Schema 与 G5 合成对象均已删除并回读为空。

G0 安全补充：在负责人授权后将直接依赖约束从 `pypdf>=5,<6` 升级为 `pypdf>=6.16.1,<7`，锁定 `pypdf 6.17.0`；`uv audit --frozen` 已报告无已知漏洞和不良项目状态，升级后的全量回归也已通过。该结果不等于生产安全认证。

边界事件：2026-09-04 对合成 PDF 的 Docling 深度转换曾触发 RapidOCR 默认模型下载；进程已停止，下载文件已精确移除。该次不属于任何真实 Gate 验收，后续必须使用显式本地模型目录或拒绝网络 Stub。
