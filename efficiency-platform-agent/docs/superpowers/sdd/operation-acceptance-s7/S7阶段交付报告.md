# S7 阶段交付报告

## 双状态判定

- `IMPLEMENTATION_READY`：已授权范围内的离线实现、治理测试、G0 回归和文档证据齐全；真实 Gate 可全部未执行。尚有未完成任务时不得提前宣告该状态。
- `REAL_ACCEPTANCE_COMPLETE`：仅当 `selected_real_gates` 非空、每个选中 Gate 均有本次授权并真实通过、清理回读为零时允许设置。
- 全 skipped、仅 Mock 或仅本地预检不得标记 `REAL_ACCEPTANCE_COMPLETE`。

## 当前结论

当前已形成离线实现与治理证据，并在 2026-09-07 完成 G1 DeepSeek Chat 的一次网络探测和一次最小计费结构化调用，以及 G3 PostgreSQL/pgvector 的真实 Schema 验收。阶段结论仍为 `IMPLEMENTATION_READY` 候选，且不能升级为 `REAL_ACCEPTANCE_COMPLETE`：G2、G4、G6、G7 尚未形成完整真实验收证据，费用由 Provider 返回为未知零值，文档用户文件、人工质量和端到端清理仍未完成。

G0 原有依赖安全风险已处理：在负责人授权后将 `pypdf` 约束升级为 `pypdf>=6.16.1,<7`，当前锁定 `6.17.0`，`uv audit --frozen` 已报告无已知漏洞和不良项目状态。该结果不等同于生产安全认证。

2026-09-07 复核：`pypdf` 是项目直接依赖，已从 5.9.0 升级并锁定到 6.17.0；升级后将重新执行完整离线回归和文档解析契约检查。

补充解析验证：`uv lock` 将 `pypdf` 解析为 6.17.0；`docling>=2,<3` 和 `pypdfium2>=5,<6` 的直接解析链未发生冲突。

## G0 最新离线证据

| 检查 | 结果 |
|---|---|
| `uv run pytest -m "not real_external" -q` | 519 passed，2 warnings，254 subtests passed，退出码 0 |
| `uv run python -m unittest discover -s tests -p "test_*.py" -q` | 249 tests，全部通过，退出码 0 |
| `uv run ruff format --check src tests scripts` | 329 files already formatted，退出码 0 |
| `uv run ruff check src tests scripts` | All checks passed，退出码 0 |
| `uv run mypy src/efficiency_platform_agent` | 169 source files 无问题，退出码 0 |
| `uv run python -m compileall -q src tests scripts` | 通过，退出码 0 |

上述命令均未连接真实外部服务。`uv audit --frozen` 当前无已知漏洞，但不得以测试全绿或安全扫描通过掩盖真实 Gate 尚未完成的边界。

补充边界事件：对合成 PDF 的 Docling 深度转换曾触发 RapidOCR 默认模型下载；进程已停止并精确移除下载文件。该行为不计入真实 Gate 通过，后续 PDF/OCR 验收必须使用显式本地模型目录或拒绝网络 Stub。

针对该事件，`DoclingParser` 已增加默认失败关闭：未显式注入本地 PDF 管线时不初始化 Docling PDF 转换器，并以 `DOCLING_PDF_PIPELINE_REQUIRED` 拒绝请求；本地 Markdown 等不需要 PDF 模型的格式保持原有路径。

S7 验收脚本已增加授权文件边界校验：仅接受版本为 `s7-authorization/1` 且通过 `GateAuthorization` 校验的记录；缺失、格式错误或空授权均返回稳定的未执行结果，不创建真实客户端、不发起外部 I/O。

最近一次零网络静态预检（`uv run python scripts/s7_preflight.py --gate all`）确认 Python 3.13 环境可用，`pypdf_security=clear`；G1 真实验收收尾后，`deepseek_chat` 已恢复 `enabled=False`，其余 `deepseek_web_search`、`postgres_pgvector`、`redis_taskiq`、`cos_artifact`、`document_pipeline` 与 `end_to_end` 也保持 `enabled=False`。预检本身未创建外部客户端、未发起网络请求、未输出配置值。

G1 真实证据：`evidence/g1-deepseek-network-probe.json` 为模型列表只读探测成功；`evidence/g1-deepseek-chat-acceptance.json` 为 `deepseek-v4-flash` 最小结构化请求成功，`network_calls=1`、`model_calls=1`、`input_tokens=114`、`output_tokens=70`。模型响应正文未写入证据，`cost_observed=false` 表示 Provider 未返回价格，不能据此推断实际费用。

G4/G5 真实证据：`evidence/g4-redis-network-probe.json` 记录两个 Redis PING 成功且 `redis_records=0`；G5 首次端点配置失败已保留 `evidence/g5-cos-network-probe-failed.json`，修正为官方区域服务端点后 `evidence/g5-cos-network-probe.json` 记录 Bucket HEAD 成功，随后 `evidence/g5-cos-artifact-acceptance.json` 记录单对象生命周期和前缀清理回读成功。G5 合成对象已删除，无长期对象残留；Redis/COS 账单均未由 SDK 返回，`cost_observed=false`。

G4 写入证据：`evidence/g4-redis-taskiq-acceptance.json` 记录 `redis_streams_unsupported`，当前目标 Redis 为 4.0.8，未创建事件或 Taskiq 队列数据，清理回读为零。

G3 真实证据：`evidence/g3-postgres-network-probe.json` 记录 Agent 自有 `jjcy_ai_vector` 的 PostgreSQL 16.11/pgvector 只读预检；`evidence/g3-postgres-pgvector-acceptance.json` 记录在临时 Runtime 逻辑 DSN 映射下完成 5 条合成记录、事务与乐观版本、跨连接 Checkpoint、1024 维向量、原生 FTS、租户过滤和精确 Schema 回滚，`cleanup_ok=true`。本次未修改 `.env`，未访问 Java 业务表；若后续提供独立 Runtime 数据库，需在该目标上重新执行 G3。

`scripts/s7_verify.py` 已与预检脚本一致地在入口处注入项目 `src` 路径；因此直接执行 `uv run python scripts/s7_verify.py` 也会返回 `NOT_EXECUTED/gate_not_selected`，无需依赖调用方设置 `PYTHONPATH`，且不执行任何外部 I/O。

G7 控制面补充：`scripts/s7_preflight.py --gate end_to_end` 已实现零网络核对所选子 Gate 的配置、授权有效期、脱敏成功证据、清理清单和固定预算；当前命令返回 `BLOCKED`，未创建任何外部客户端，原因已在结果中结构化记录。

清理边界补充：`scripts/s7_cleanup.py` 已对 PostgreSQL Schema、Redis 前缀和 COS 前缀执行精确格式校验，拒绝通配符、`FLUSHDB`、`FLUSHALL` 和全局目标；外部清理仍需在 G7 获得动作授权后执行。

G2 控制面补充：`s7_verify.py` 已提供服务端 `web_search` 的独立探测与计费验收入口，复用 S5 ResearchProvider 契约，不打开来源 URL、不保存网页正文；当前因项目禁止付费商业热点数据源，Gate 保持关闭，未执行真实搜索。

## 仍未完成的验收项

- G1 与 G3 已完成本次授权范围内的真实验收；G2、G4、G6、G7 仍未完成，不得以 G1/G3 的局部通过或离线 Stub 代替全部真实验收。
- G2 受项目约束不启用付费商业数据源；G3 已在 Agent 自有向量库的临时 Runtime 逻辑映射下通过，但 `.env` 仍缺少独立 `AGENT_RUNTIME_DATABASE_URL`，G7 不能复用该临时映射直接宣称完成；G4 已完成两个 Redis 目标的只读 PING 探测，但真实写入在能力预检阶段因 Redis `4.0.8` 不支持 Streams `XADD` 而失败关闭，尚未进入 Worker；G5 Bucket HEAD 和单对象写入生命周期已通过，G6 仍需受控文件人工样本。上述 Gate 均已恢复关闭。
- 复杂 PDF/Office 的 Docling 页级质量、真实本地 PaddleOCR 召回率、用户文件人工验收尚未完成。
- Redis/Taskiq、搜索的真实写入/计费、G7 端到端清理尚未完成；G3 PostgreSQL 合成 Schema 生命周期和清理回读已通过，COS 本次合成对象生命周期和清理回读也已通过；DeepSeek G1 的价格回传仍未知。Redis G4 的当前阻断条件已明确为目标服务版本不支持 Streams。
- 已在本轮全量回归中确认 `pypdf 6.17.0` 与现有 Docling/PDF 解析路径的离线契约兼容性；当前不再存在 `pypdf 5.9.0` 的已知漏洞阻断。真实复杂 PDF/OCR 质量仍需人工样本 Gate。

## Gate 证据记录模板

每个 Gate 必须记录：Gate 名称、授权类型、批准人、执行者、目标与输入摘要、模型/资源标识、调用/写入上限、命令、结果、证据位置、cleanup owner、deadline、清理命令和回读结果。Secret、连接串、签名 URL、用户正文和网页原文不得进入报告。

## 人工样本

人工验收至少覆盖行业研究、多平台内容、品牌运营和活动策划；每份交付物执行十项评价维度、通过/需修改/拒绝三态和最多两轮修订，失败与部分范围必须保留。
