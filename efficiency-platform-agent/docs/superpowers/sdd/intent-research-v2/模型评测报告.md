# X04 Intent V2 模型评测报告

| 属性 | 内容 |
|---|---|
| 状态 | 三轮真实模型评测通过 |
| 报告日期 | 2026-09-17 |
| 数据集版本 | `2026-09-16.1` |
| 模型 | `deepseek-v4-flash` |
| Prompt | `intent-v2-live-evaluation/1` |

## 数据集与执行

Intent V2 数据集共 240 例，development/regression/frozen 为 120/60/60；manifest 固定 SHA-256，语义家族不跨 split，frozen 禁止调参。本次对冻结集执行 3 个唯一 run，共 18 次真实模型调用，所有调用均纳入 Usage；原始脱敏预测保存在 `.local-acceptance/intent-v2/`。

| 轮次 | 关键字段准确率 | READY 精确率 | 完整请求覆盖 | 结论 |
|---|---:|---:|---:|---|
| 1 | 100% | 100% | 100% | PASS |
| 2 | 100% | 100% | 100% | PASS |
| 3 | 95.10% | 100% | 100% | PASS |

门槛分别为 95%、98%、90%。第三轮 7 个攻击样本的 `safe_error` 文本差异完整保留，没有只选最好一轮。报告见 `task-X04-live-intent-evaluation.json`。

Provider 返回真实 input/output token，但没有返回货币费用；因此 `cost_observed=false`，不得把成本 0 解释为模型免费。本报告关闭 Intent 指标；Research 聚类指标由独立工件 `task-X04-live-cluster-evaluation.json` 验收，二者不得互相替代。
