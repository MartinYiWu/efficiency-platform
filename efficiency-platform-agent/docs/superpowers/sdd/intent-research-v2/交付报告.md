# Intent / Research V2 阶段交付报告

| 项目 | 当前状态 |
|---|---|
| C01—C03、I01—I07、R01—R10、X02—X03 | 离线通过 |
| X01 | 集成通过（临时隔离 PostgreSQL） |
| X04 | 真实验收通过（含合成冻结聚类盲测） |
| X05 | 真实验收通过；Agent 侧阶段关闭 |

## 已交付

Agent 侧已具备语义 Intent V2、多轮修正、时间窗口、ResearchBrief、免费来源目录、安全传输、RSS/Atom/HN 采集、过滤去重、事件/Claim/Evidence、质量门禁、有界补采、PostgreSQL 权威状态、预算租约、UNKNOWN_OUTCOME、保留策略、单租户灰度和回滚。

真实验证包括：3 个免费公开源、Intent 3 轮 18 次模型调用、X04 五类链路、X05 五类各 4 次共 20 请求、硬停止后零新增调用，以及 100 文档/30 事件确定性合成盲测的 15 次真实模型调用。聚类 precision/recall 均为 100%，120 个 gold pair 无误合并/漏合并；该结果明确不外推真实世界精度。

## 不包含事项

- 没有修改生产配置、部署服务、执行共享 DDL、扩大租户或发布前端。
- 没有 Git 提交/推送；项目规范永久禁止所有 Git 操作。
- 合成聚类基准没有人工标签与统计显著性，不能解释为真实新闻 100% 准确。
- Provider 未返回货币成本，报告的 0 不是“免费”。

## 最终验证

新增聚类代码后最终复测：`uv run python -m pytest -q` 为 1544 passed、9 skipped、280 subtests；X01 临时 PostgreSQL 集成为 9 passed；聚类单测+治理+架构组合为 138 passed、246 subtests（其中治理/架构 129 项）；Ruff 全 `src/tests/scripts`、mypy 299 个源码/脚本文件、compileall 均退出 0。Windows 下使用 `uv run python -m pytest`，因为现有 `pytest.exe` 启动器仍指向项目迁移前旧路径。
