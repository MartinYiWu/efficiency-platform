# 依赖与开源准入记录

| 属性 | 内容 |
|---|---|
| 状态 | C01 离线依赖准入已验证；真实来源未准入 |
| 负责人 | Agent 端维护负责人 |
| 适用范围 | Python 3.13 离线解析依赖与固定提交机制参考；不批准出网 |
| 更新时间 | 2026-09-16 |
| 关联决策 | [ADR-0002](../../../adr/ADR-0002-语义意图与多源研究闭环.md)、[C01 报告](task-C01-report.md) |

## 锁定组合与证据

解释器 Python 3.13.15（Windows）；uv 0.12.9；`uv lock` 输出仅新增 12 个包，未报告现有包升级。下表版本来自 `uv.lock` 生成结果与安装元数据 `importlib.metadata` 双重检查；许可来自已安装包的 `License-Expression` 或 `License` 元数据，并非仅凭名称推测。锁文件采用版本范围及精确解析版本，后续升级须重新审查。

| 直接运行依赖 | 锁定版本 | 元数据许可证 | 运行时直接依赖（元数据，省略未选 extras） | 用途/边界 |
|---|---|---|---|---|
| feedparser | 6.0.14 | BSD-2-Clause（License） | feedparser-sgmllib >=2,<3 | 纯内存 RSS/Atom 解析；不赋予外网权限 |
| Trafilatura | 2.2.0 | Apache-2.0（License-Expression） | certifi、charset-normalizer >=3.4.9、courlan >=1.4、htmldate >=1.10、justext >=3、lxml >=6.1.1、urllib3 >=1.26,<3 | 离线 HTML 正文；不自动抓站或绕过来源准入 |
| dateparser | 1.4.3 | BSD-3-Clause（License-Expression） | python-dateutil >=2.7、pytz >=2024.2、regex >=2024.9.11、tzlocal >=0.2 | 有显式基准时钟和时区的中文相对时间；歧义仍由后续 TemporalResolver 校验 |
| HTTPX | 0.28.1 | BSD-3-Clause（License） | anyio、certifi、httpcore 1.x、idna | 原 dev 依赖升为直接运行依赖；C01 仅导入，不出网 |
| tzdata | 2026.3 | Apache-2.0（License） | 无 | Windows IANA 时区作为稳定直接运行依赖，尽管 tzlocal 已按 Windows 平台传递提供 |

`uv sync --frozen` 后新增传递包：babel 2.18.0（BSD-3-Clause）、courlan 1.4.0（Apache-2.0）、feedparser-sgmllib 2.1.0（PSF-2.0）、htmldate 1.10.0（Apache-2.0）、justext 3.0.2（元数据“The BSD 2-Clause License”）、lxml-html-clean 0.4.5（BSD-3-Clause）、pytz 2026.3.post1（MIT）、tld 0.13.2（MPL-1.1 OR GPL-2.0-only OR LGPL-2.1-or-later）、tzlocal 5.4.4（MIT）。其中 tld 为多重许可表达式；后续若分发/复制需选择并复核对应许可义务。原环境已有公共传递包不在这份“新增”列表；以锁文件为完整依赖树。

准入测试直接导入五项依赖，在内存中完成 RSS 与 Atom 字节解析、HTML 字符串正文提取、带 `RELATIVE_BASE` 和 `Asia/Shanghai` 的“昨天”解析、`ZoneInfo` 查询；4 passed。测试没有 HTTP 请求、真实来源或配置值读取。仅证明最小样例和运行环境，不证明任意网页质量、日期歧义、来源条款或网络安全。真实出网需 R01/R02/X04 单独准入。

## 固定提交机制参考与复制边界

沿用[总览设计 O1—O7](../../specs/2026-09-16-Agent意图与研究闭环优化总览-设计.md)的固定提交、路径和本地映射；该设计记录已读取固定提交许可文件开头。以下只借鉴机制，不引入对方 Runtime 或代码：

| 参考项目及提交 | 映射到本地任务 | 边界 |
|---|---|---|
| Graphon c2f8bf2663445d6f678ddc9460eead1d8c7fb8f2（Apache-2.0） | I04 分类/参数提取、I05 Schema 校验 | 不引入 Graphon 引擎，不沿用未命中回退首类 |
| Coze Studio fefb05ff27be1da939612fbf9faf5db62583b8ae（Apache-2.0） | I04/I05 任务相关历史、Other 分支 | 不引入 Go/Eino 和第二 Runtime |
| Rasa 60a3cff9c08183760355b07bd60f5223d8916d6b（Apache-2.0） | I02/I05 多轮槽位与任务隔离 | 不引入 Tracker/Runtime |
| Open Deep Research 1b7d2e80db9faa586165c60e09096dbbfd483a64（MIT） | R09 Supervisor/Researcher 有界循环 | 不携带付费搜索依赖，不绕过程序化质量门禁 |
| TrendRadar 792bcc3928b1617bba09df34989fd5675c159b86（GPL-3.0） | R01/R03/R05/R06 来源、筛选、排名机制 | 只参考机制；不得复制 GPL 代码 |
| Daybreak 9fbb3ebea2b31ac0cf72a38f157845d73e455308（README 声明 MIT、根目录缺独立 LICENSE） | R03/R05/R06/R10 两阶段去重与交付 | 只参考机制；缺许可依据不得复制代码 |

上游固定提交映射是设计记录，不代表本轮重新联网核验许可证；如拟复制任何外部代码、发布或变更许可，先重新核对上游许可文件并履行 NOTICE。Dify 与 GDELT 的细节仍见总览 O2/O8，本轮未引入运行依赖。

## 安全扫描观察

`uv audit --frozen` 在 uv 0.12.9 中可用，但退出码 1：228 包发现 `accelerate 1.14.0` 两条同一 CVE-2026-69112 的通告（PYSEC-2026-3804、GHSA-4j2p-28q2-5m79），工具显示无修复版本。该包不是本次新增的 12 个包；不得宣称全量安全扫描通过。由现有依赖维护者评估 sharded checkpoint weight_map 路径风险，修复发布前避免处理不可信 checkpoint，不因 C01 擅自升级无关依赖。
