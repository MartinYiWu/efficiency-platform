# 运营 Agent 专业化交付与质量闭环进度

| 属性 | 内容 |
|---|---|
| 状态 | 实施中；Task 1–5 已独立复核通过，Task 6 待独立复核 |
| 负责人 | Agent 平台实施负责人 |
| 适用范围 | 纯 Agent 侧运营交付契约、质量闭环与离线测试 |
| 更新时间 | 2026-09-17 |
| 关联计划 | [运营 Agent 专业化交付与质量闭环实施计划](../plans/2026-09-17-运营Agent专业化交付与质量闭环-实施计划.md) |

> 本账本按任务顺序只追加记录，不覆盖历史。本项目永久不是 Git 仓库；所有任务均以文件快照、命令输出和本账本作为变更证据。

## Task 1：冻结 Deliverable V2 契约与交付类型映射

### 实施结果

- 新增基于 `OperationSpecialistCapabilityId` 唯一目录的 11 项能力到 5 类 V2 交付类型映射；未知能力以 `DELIVERABLE_KIND_UNSUPPORTED` 失败关闭。
- 新增排名摘要、平台内容、行动方案、诊断和复盘五类具体内容与交付模型，并分别以 `kind` 和 `deliverable_kind` 作为判别字段。
- 新增 V2 摘要、来源、后续动作、provenance 和集合契约；所有 V2 模型统一使用 `extra="forbid"` 与 `frozen=True`。
- `NextActionV2.intent_patch` 复用严格 `JsonValue`，拒绝 datetime 等非 JSON Python 对象并隔离输入快照。
- provenance 校验非负计数、已核验来源数上界、成对 UTC aware 时间窗及严格递增时间区间。
- 新增 `DeliverableSet` 版本判别联合，仅由 `contract_version` 在 V1/V2 间分派。

### RED 证据

命令：

```powershell
.venv\Scripts\python.exe -m pytest tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py -q
```

结果：退出码 `1`，收集阶段按预期失败。关键输出：

```text
ImportError: cannot import name 'DeliverableSet' from 'efficiency_platform_agent.contracts.deliverables'
ModuleNotFoundError: No module named 'efficiency_platform_agent.agents.operation.delivery_kind'
2 errors in 0.66s
```

### GREEN 与回归证据

目标测试：

```powershell
.venv\Scripts\python.exe -m pytest tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py -q
```

结果：退出码 `0`，`18 passed in 0.47s`。

最终契约回归：

```powershell
.venv\Scripts\python.exe -m pytest tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py tests/contracts/test_stream_event_contracts.py -q
```

结果：退出码 `0`，`38 passed in 0.48s`。

全量回归：

```powershell
.venv\Scripts\python.exe -m pytest -q
```

结果：退出码 `0`，`1585 passed, 9 skipped, 2 warnings, 280 subtests passed in 54.71s`。两条 warning 来自既有 Starlette deprecated alias 与 Polars Excel future behavior，不在 Task 1 修改路径内。

质量门禁：

```powershell
.venv\Scripts\python.exe -m pytest tests/architecture/test_dependency_rules.py -q
.venv\Scripts\python.exe -m compileall -q src tests
.venv\Scripts\python.exe -m ruff check src/efficiency_platform_agent/contracts/deliverables.py src/efficiency_platform_agent/agents/operation/delivery_kind.py tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py
.venv\Scripts\python.exe -m ruff format --check src/efficiency_platform_agent/contracts/deliverables.py src/efficiency_platform_agent/agents/operation/delivery_kind.py tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py
.venv\Scripts\python.exe -m mypy src/efficiency_platform_agent/contracts/deliverables.py src/efficiency_platform_agent/agents/operation/delivery_kind.py tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py
```

结果依次为：`12 passed, 47 subtests passed`；源码编译退出码 `0`；`All checks passed!`；`4 files already formatted`；`Success: no issues found in 4 source files`。

### DeliverableSetV1 保持性检查

- `CitationV1`、`DeliverableV1`、`DeliverableSetV1` 三个类的原有字段、约束、默认值与类体未修改；V2 代码追加在 V1 定义之后。
- AST 源码快照检查：`V1_CLASS_COUNT=3`，三类合并源码 SHA256 为 `37E7B7906FA293619EE439D92D6EFE81A3B2DC0134C6F38D3EE146F8E4F260A4`。
- 既有 `tests/contracts/test_stream_event_contracts.py` 单独回归为 `20 passed in 0.44s`；联合回归亦通过。

### 文件快照清单

| 文件 | SHA256 / 状态 |
|---|---|
| `src/efficiency_platform_agent/contracts/deliverables.py` | `6F32D06E8613D224C391448476FFBA42F139512B6613D322741E0AEDF1A8BD49` |
| `src/efficiency_platform_agent/agents/operation/delivery_kind.py` | `3135FF11B726FE899352E1E826A37F2E9D82494674036F574A872B63D14B894A` |
| `tests/contracts/test_deliverable_v2_contracts.py` | `6ED00B2FD1996ED8990185D7C6515112809EFCC46B19C442DAA38E3FC0C1567E` |
| `tests/unit/agents/operation/test_delivery_kind.py` | `17875DA7F64EA7EF75FB0BC9CF0904B94F1DDC9DB32B18AE0DE0D9ECE90E6F60` |
| 本进度账本 | 已创建；后续任务只追加 |
| `docs/superpowers/progress/sdd/agent-task-1-report.md` | 已创建独立实施报告 |

### 边界与遗留

- 本任务仅冻结离线契约与映射，未接入网络、Provider、数据库、Java/UI 或生产运行时。
- 未执行任何 Git 命令，也未创建提交。
- 任务内无阻断担忧；全量回归保留两条既有依赖 warning，后续应由对应组件任务治理。

### 独立复核

- Task 1：complete（non-Git 文件快照基线 → 当前文件，规格复核通过，代码质量 Approved，无 Critical/Important/Minor 问题）。
- 静态复核无法独立重现 TDD 执行时序；RED/GREEN 原始摘要保存在本账本与 `docs/superpowers/progress/sdd/agent-task-1-report.md`。

## Task 2：实现确定性展示验证、复制文本和 V1 投影

### 实施结果

- 新增 `DeliveryPresentationValidator`，校验排名摘要的引用闭包与连续排名，并从结构化内容重算 `copy_text`，不接受模型另写的不同正文。
- 新增五类固定文本渲染：排名摘要、平台内容、行动计划、诊断和复盘均使用确定标题、编号、换行和字段顺序，不调用模型。
- 新增集合校验：交付物 ID 唯一、研究时间窗与排序口径完整、动作目标存在、条目目标属于目标排名摘要。
- 不同平台 `body_markdown` 经 NFKC 与空白标准化后计算 SHA-256；相同哈希以 `DELIVERY_PLATFORM_CONTENT_DUPLICATED` 失败关闭。
- 集合 `degraded` 仅从已有运行降级事实、摘要完整性及结构化 warning 派生；集合 warning 按首次出现顺序去重，不读取正文推断质量。
- 新增 V2 到 V1 单向投影，只保留 V1 明确拥有的复制文本、平台字段、引用和交付物 warning code，不反推 V2 来源质量、排名或动作。

### RED 证据

命令：

```powershell
.venv\Scripts\python.exe -m pytest tests/unit/capabilities/quality/test_deliverable_v2.py -q
```

结果：退出码 `1`，收集阶段按预期失败。关键输出：

```text
ModuleNotFoundError: No module named 'efficiency_platform_agent.capabilities.quality.deliverable_v2'
1 error in 0.42s
```

### GREEN 与回归证据

目标测试首次 GREEN：

```powershell
.venv\Scripts\python.exe -m pytest tests/unit/capabilities/quality/test_deliverable_v2.py -q
```

结果：退出码 `0`，`19 passed in 0.43s`。

Task 2、V1 hashtags、V2 契约与类型映射联合回归：

```powershell
.venv\Scripts\python.exe -m pytest tests/unit/capabilities/quality/test_deliverable_v2.py tests/unit/capabilities/quality/test_deliverable_hashtags.py tests/contracts/test_deliverable_v2_contracts.py tests/unit/agents/operation/test_delivery_kind.py -q
```

结果：退出码 `0`，`38 passed in 0.52s`。

全量回归：

```powershell
.venv\Scripts\python.exe -m pytest -q
```

最终结果：退出码 `0`，`1604 passed, 9 skipped, 2 warnings, 280 subtests passed in 54.42s`。首次全量运行的唯一失败来自控制器复核快照嵌套进仓库后产生的文档相对链接污染；控制器将快照移出仓库后，同一命令重跑通过，Task 2 实现未为该外部失败修改行为。

质量门禁：

```powershell
.venv\Scripts\python.exe -m pytest tests/architecture/test_dependency_rules.py -q
.venv\Scripts\python.exe -m compileall -q src tests
.venv\Scripts\python.exe -m ruff check src/efficiency_platform_agent/capabilities/quality/deliverable_v2.py src/efficiency_platform_agent/capabilities/quality/__init__.py tests/unit/capabilities/quality/test_deliverable_v2.py
.venv\Scripts\python.exe -m ruff format --check src/efficiency_platform_agent/capabilities/quality/deliverable_v2.py src/efficiency_platform_agent/capabilities/quality/__init__.py tests/unit/capabilities/quality/test_deliverable_v2.py
.venv\Scripts\python.exe -m mypy src/efficiency_platform_agent/capabilities/quality/deliverable_v2.py src/efficiency_platform_agent/capabilities/quality/__init__.py tests/unit/capabilities/quality/test_deliverable_v2.py
```

结果依次为：`12 passed, 47 subtests passed in 2.22s`；源码编译退出码 `0`；`All checks passed!`；`3 files already formatted`；`Success: no issues found in 3 source files`。

### V1 投影边界

- 集合摘要只取 `summary.message`，降级只取组装完成的 `set_v2.degraded`。
- 交付正文只取确定性 `copy_text`；只有 `platform_content` 投影已有 hashtags 与 format_notes。
- 引用只映射 URL、标题和来源；warning 只映射交付物现有 code。
- 不调用 V1 assembler，不从正文生成来源质量、热点排名、动作、hashtag 或新 warning。

### 文件快照清单

| 文件 | SHA256 / 状态 |
|---|---|
| `src/efficiency_platform_agent/capabilities/quality/deliverable_v2.py` | `1BCD83E1B3DE9DE8BF4C1A6B5CEC132F386057EBE5C7BCC1AE5F180CE1C67742` |
| `src/efficiency_platform_agent/capabilities/quality/__init__.py` | `1B571FBB8BEAE98CBE847AD92298BC11B77017DECB27E8E3F69FDBB7EAAD11A6` |
| `tests/unit/capabilities/quality/test_deliverable_v2.py` | `1F2BB70E2C22D1C5A3F37A0B47B3A8C30C878E45A3DA6600E207E3D20F9F80C2` |
| 本进度账本 | 已追加 Task 2；Task 1 历史未覆盖 |
| `docs/superpowers/progress/sdd/agent-task-2-report.md` | 已创建独立实施报告 |

### 边界与遗留

- Task 1 的 V2 契约与 `delivery_kind.py` 均未修改；未接入模型、Provider、网络、数据库、Java/UI 或生产运行时。
- 未执行任何 Git 命令，也未创建提交。
- Task 2 无阻断担忧；全量回归保留两条既有依赖 warning，后续由对应组件任务治理。

### Task 2 独立复核

- Task 2：complete（non-Git 文件快照基线 → 当前文件，规格复核通过，代码质量 Approved，无 Critical/Important/Minor 问题）。
- 静态复核无法独立重现 TDD 执行时序；RED/GREEN 原始摘要保存在本账本与 `docs/superpowers/progress/sdd/agent-task-2-report.md`。

## Task 3：注册 V2 Prompt 与模型类型化生成

状态：DONE_WITH_CONCERNS。Task 3 范围目标测试和静态门禁通过，全量运行链尚未通过，详见 [Task 3 实施报告](sdd/agent-task-3-report.md)。永久 non-Git，无提交。

### 实施与接口裁决

- 显式注册 V1/V2 Prompt，V2 ID 为 `operation.deliverable.generation/2`。
- 五类模型草稿 Schema 名称为 `operation_ranked_digest`、`operation_platform_content`、`operation_action_plan`、`operation_diagnosis`、`operation_retrospective`；正式 Task 1 契约不改。
- 协调方明确私有 Draft Schema 的 citations/copy_text/warnings 为空，运行时构建正式 V2 并通过 Task 2 渲染与验证；保守来源固定 public_page/secondary/unverified，ID、日期、hostname 与反向 supports_item_ids 均确定性派生。
- 版本、引用、权限和核验升级错误失败关闭；只允许结构错误沿剩余迭代、Token、费用与超时修复一次，第二次截断也不再调用。
- 质量报告仅使用确定性 V1 投影，既有结果外壳不变，payload 保持 V2。

### RED 与 GREEN

```powershell
.venv\Scripts\python.exe -m pytest tests/unit/agents/operation/specialists/test_model_backed_v2.py tests/unit/prompts/test_prompt_runtime.py -q
```

首轮 RED：`19 failed, 8 passed in 3.12s`，预期为注册工厂接口不存在。补充边界 RED：同一 Specialist 文件单独执行 `3 failed, 22 passed in 2.23s`，分别证明严格排名类型、核验权限及混合 V1 伪造引用错误需要修复。

```powershell
.venv\Scripts\python.exe -m pytest tests/unit/agents/operation/specialists/test_model_backed_v2.py tests/unit/agents/operation/specialists/test_model_backed_citations.py tests/unit/prompts/test_prompt_runtime.py -q
```

GREEN：`35 passed in 1.78s`。AST/契约/映射/Task 2 联合 `49 passed, 47 subtests passed in 3.40s`；compileall 退出码 0；四个修改 Python 文件 Ruff、格式和 mypy 均通过。完整命令与五文件 SHA256 清单保存在 Task 3 报告。

### 全量回归与担忧

`.venv\Scripts\python.exe -m pytest -q`：退出码 1，`25 failed, 1605 passed, 9 skipped, 2 warnings, 280 subtests passed in 63.95s`。

24 项涉及默认 V2 与既有 V1 fake output/运行链未迁移，包括旧测试要求第三次模型调用而本任务明确只修复一次；需要 Task 4 及后续端到端迁移逐项验证，不得把 Task 3 单元通过等同于整个运行链可用。另 1 项由协调方同步计划文档的省略号示例触发占位符治理失败，已反馈协调方。未越过清单修改集成测试、聚合、Java/UI 或数据库。

### Task 3 阶段兼容裁决与最终验证

协调方随后同步 Task 3 brief：本阶段仅增加显式 V2 选择，构造器及组合根默认保持 V1，Task 4 聚合完成后再切默认；唯一结构修复响应若为 `PROVIDER_RESPONSE_TRUNCATED`，允许在剩余预算内重传相同修复消息一次，不生成第二个修复 Prompt。协调方同时修复计划文档占位符。

据此追加测试先得到 RED：`2 failed, 25 passed in 4.14s`，分别证明默认 V1 与截断传输重试尚未实现。修复并兼容既有免费模型零费用预算后，目标测试加完整运营集成 `69 passed in 2.51s`。正费用额度已消耗完仍禁止修复；初始零费用额度保持原样交由 ModelRuntime 执行真实费用准入。

最终全量命令 `.venv\Scripts\python.exe -m pytest -q`：退出码 0，`1632 passed, 9 skipped, 2 warnings, 280 subtests passed in 56.09s`。先前 25 项失败全部消失。更新后的 Ruff、格式、mypy 四文件门禁和源码编译通过；最终五文件 SHA256 已更新到 Task 3 报告。

Task 3 最终状态：DONE，无提交，未改 Task 4 文件；V2 端到端聚合及默认切换仍由 Task 4 承担。真实 Provider/网络/数据库/生产验收未执行。

### Task 3 独立复核修复：保留 URL 路径边界

独立复核提出 Important：`/news/?id=1` 与 `/news?id=1` 被无条件去尾斜杠合并，导致 Article A 元数据与 evidence-b ID/日期混合。已先补有/无 EvidencePack 的 URL 对照及元数据整组来源回归，再做最小修复：保留完整 path；匹配证据时 title/source/id/date 一起取自该 EvidenceRecord。

RED：`.venv\Scripts\python.exe -m pytest tests/unit/agents/operation/specialists/test_model_backed_v2.py -k 'path_trailing_slash or evidence_metadata_is_selected' -q`，退出码 1，`3 failed, 27 deselected in 2.34s`。实际失败输出复现 evidence-b 与 Article A 混合。

GREEN：同一 Specialist 文件完整运行 `30 passed in 2.02s`；原 Task 3 三文件聚焦套件 `40 passed in 2.21s`；AST 守卫 `12 passed, 47 subtests passed in 2.16s`；compileall、Ruff、format、mypy 均通过。完整命令和更新后的两个 SHA256 已追加到 Task 3 报告。本次局部修复没有重跑全量；前述 1632 条全量通过仍是修复前历史证据。

修复状态：DONE，无提交；只修改 Task 3 实现、测试、报告与本账本。

### Task 3 独立复核结论

- Task 3：complete（首轮复核发现 1 个 Important 来源身份合并问题，TDD 修复后复审通过）。
- 最终规格复核通过，代码质量 Approved；无剩余 Critical/Important/Minor 问题。
- 复审批注：修复后聚焦、架构、文档和静态门禁通过；修复后的全量回归留待 Task 4 完整聚合变更后统一执行。

## Task 4：首批 RED 与接口边界核对

状态：NEEDS_CONTEXT。新增 V2 聚合集成测试并运行 `.venv\Scripts\python.exe -m pytest tests/integration/test_operation_deliverable_v2.py -q`，退出码 1，`4 failed in 3.20s`，分别验证默认 V2、部分结果、V1 投影和版本白名单尚未实现。未修改生产代码，新 Run 默认仍为 V1。

已向协调方报告：brief 的 V1 Specialist 版本与仅从完成 V2 投影要求冲突；Research V2 旧适配器丢失终态及 provenance；研究委托默认 V1；旧质量门只认 V1；空 bundle 被改为 FAILED；旧 V1 集成测试迁移不在白名单。待裁定必要窄改范围及版本语义后继续 GREEN。证据和边界详见 [Task 4 实施报告](sdd/agent-task-4-report.md)。永久 non-Git，无提交；未声称实现或全量验证完成。

### Task 4 裁定落地与最终验证

协调方已同步 Task 4 brief 并批准必要研究 adapter/Specialist、场景质量门、空结果规则、local_real_factory 版本薄透传与旧 V1 测试显式配置。最终实现固定新 Run 默认 `deliverable-set/2`；显式 V1 保持 native `deliverable/1` 与 V1 assembler，同一 Run 不混生成版本。V2 质量视图使用已完成 V2 向 V1 的确定性投影。

研究 V2 通过既有中央 Supervisor 调度一次服务，每 Run 独立 adapter 实例及独立结果观察器保留 brief/outcome；两个并发 Run 的 PARTIAL/NO_MATCHES、来源数与各自窗口隔离测试通过。COMPLETE/PARTIAL 保留已接纳事件，PARTIAL 给具体缺口 warning；NO_MATCHES 返回成功空集合；FAILED 不生成伪交付。V2 Run 输出携带 `delivery_contract_version`，保持原 SSE 事件序列。

版本兼容矩阵：默认 V2 + Research V2 adapter 支持完整交付；显式 V1 + 旧 ResearchProvider 保持既有行为；V2 + 仅有非空 V1 ResearchResult 的旧 Provider 在内容模型调用前返回 `RESEARCH_UNAVAILABLE`，不能猜造治理时间窗和排序。`build_local_agent_application(settings, deliverable_set_contract_version="deliverable-set/1")` 是明确回退开关。EMPTY 仍受控返回空结果，来源计数为 0。

provenance 的来源数、时间窗和排序来自可信交付及冻结 brief；原 ResearchOutcome 未暴露上游全量候选/淘汰计数，现有 candidate/merged/retained 仅按已接纳交付集合计算，eliminated=0 仅表示交付集合未再淘汰，不冒充采集漏斗全量。引用 conservatively unverified，verified_source_count=0。

RED→GREEN：扩展研究终态测试 `9 failed, 5 passed` → `14 passed`；质量 WARNING、local_real_factory 透传和旧研究端口提前失败分别有独立 RED。指定主链/补采回归 `59 passed`；V2/adapter/AST 组合 `30 passed, 47 subtests passed`；最后三个聚焦文件 `46 passed`。完整命令、先后顺序和 15 文件 SHA256 均保存在 [Task 4 实施报告](sdd/agent-task-4-report.md)。

最终 `.venv\Scripts\python.exe -m pytest -q`：退出码 0，`1650 passed, 9 skipped, 2 warnings, 280 subtests passed in 54.68s`。9 个修改生产文件 mypy 通过，15 文件 Ruff check/format 通过，源码编译通过。扩大到旧测试依赖链的 mypy 尚未通过，不声称该范围类型检查完成；未为此改动无关测试或配置。

Task 4 状态：DONE_WITH_CONCERNS，无提交。担忧为上述已裁定兼容边界、计数口径及旧测试类型检查范围；真实 Provider/网络/数据库/生产验收未执行。生产修改共 9 文件、测试共 6 文件，均在最终批准白名单内。

### Task 4 独立审查修复：研究准入与 NO_MATCHES 可信授权

独立审查指出两个 Important：旧研究端口可通过 requires_research 多平台路径生成无 provenance 的 V2；warning-only Specialist 可伪装 NO_MATCHES 成功空集合。已先补真实 Supervisor 回归得到 RED：`.venv\Scripts\python.exe -m pytest tests/integration/test_operation_deliverable_v2.py -k 'platforms_require_governed or warning_cannot_authorize' -q`，`4 failed, 13 deselected in 3.66s`。

修复后的准入矩阵替代前述 EMPTY 描述：所有 V2 行业摘要或 requires_research 多平台请求必须由组合根显式声明 governed Research V2 能力，否则启动 Supervisor 前失败 `RESEARCH_UNAVAILABLE`，研究和模型调用都为 0。V1 保持旧端口兼容。只有当前 Run 的可信 ResearchOutcomeV2(NO_MATCHES) 与 request_id、tenant/task/run、brief digest、空事件全部匹配，才能将行业摘要的缺少 bundle 结果恢复为 COMPLETE。没有事实、错 Run、错 request 或只有 warning/EMPTY 都不能授权空 V2。

ScenarioPackService 已撤销 warning 特例；OperationAgent 不按 Provider 类名或输出字段猜能力；adapter 的 request_id/brief/outcome 均记录在每 Run 独立 OperationUsage。合法 adapter NO_MATCHES 与并发隔离回归继续通过，无第二条研究编排链。

GREEN：Task 4 V2 集成文件 `17 passed in 3.51s`；Task 4 主链、旧 V1、Supervisor、Research V2 补采与 adapter 组合 `74 passed in 7.82s`；修复后最终全量 `.venv\Scripts\python.exe -m pytest -q` 为退出码 0，`1654 passed, 9 skipped, 2 warnings, 280 subtests passed in 64.90s`。9 生产文件 mypy、15 文件 Ruff/format、源码编译均通过；AST `12 passed, 47 subtests passed in 2.04s`。

两项 Important 的实施修复完成，待独立复审。仅改原批准范围的 5 个生产文件及已有 V2 集成测试；报告内的兼容矩阵、测试证据及 SHA256 已更新。无 Git、无提交；原计数口径和旧测试 mypy 范围担忧保持明示。

### Task 4 独立复核结论

- Task 4：complete（首轮复核发现 2 个 Important，TDD 修复后复审通过）。
- 最终规格复核通过，代码质量 Approved；无剩余 Critical/Important/Minor 问题。
- 旧 ResearchProvider 仅显式 V1、provenance 仅表示已接纳集合、测试依赖链 mypy 不属于生产源码门禁，均作为已披露且可接受边界保留。

## Task 5：安全阶段、单一交付事件与受控后续动作

状态：DONE_WITH_CONCERNS。完整证据与四文件 SHA256 见 [Task 5 实施报告](sdd/agent-task-5-report.md)。永久 non-Git，无提交。

Task 5 沿用 `run.stream.event/1` 和原 `phase_started`、`deliverable`、`stream_done` 事件，不新增第二套流协议或动作执行事件。运营研究请求的用户阶段固定为 `understanding_request` → `collecting_sources` → `checking_evidence` → `creating_content` → `checking_delivery`；无研究请求跳过来源收集与证据检查，只输出理解、内容创作、交付检查。非运营策略不发布运营阶段，同一 Run 每个阶段最多一次。

流事件契约拒绝内部/未知 `phase`；可选 `completed`、`target` 只允许非 bool 的非负整数，且完成数不得大于目标数。Task 4 声明版本的 V2/V1 集合在 SSE 发布前按对应 Pydantic 契约重校验；非法后续动作不会产生 `deliverable`，只产生固定 `DELIVERABLE_SET_INVALID` 呈现错误，原生命周期仍唯一输出 `stream_done`。`next_actions` 只存在于 `DeliverableSetV2` 内，不能绕过下一轮意图管线。

RED：阶段/API 为 `3 failed, 1 passed`；契约为 `11 failed, 20 passed`；撤回重校验后非法动作边界为 `1 failed, 4 deselected`。GREEN：非法动作聚焦 `1 passed, 4 deselected`；brief 指定 API/SSE/重放/契约组合 `58 passed in 4.28s`。

Ruff check、Ruff format、两生产文件 mypy 与源码编译均通过；AST 为 `12 passed, 47 subtests passed in 2.46s`。最终全量 `.venv\Scripts\python.exe -m pytest -q`：退出码 0，`1670 passed, 9 skipped, 2 warnings, 280 subtests passed in 54.47s`。两条 warning 为既有第三方弃用/未来行为提示。未执行真实 Provider、网络、数据库、前端联调或生产 SSE 验收，不声称生产上线完成。

### Task 5 R2 独立复核修复：真实节点进度与严格发布边界

首轮独立复核未通过，指出四项必须关闭的问题：Harness 文本关键词/整图返回伪阶段、双版本缺失的交付字典旁路、EventHub 递归敏感键不完整、进度计数依赖 phase 才校验。R2 已完成实现修复，当前状态为 `DONE_WITH_CONCERNS`、待独立复核；本节不提前声明规格或代码质量批准。

R2 删除 Harness 的研究关键词推断和图后批量阶段。新增按 Run 隔离的 ContextVar 窄端口：`OperationRuntime` 在合法 Submission 解析后报告理解阶段；`ScenarioSupervisorAdapter` 在真实 provider/研究任务与非研究创作波次边界报告研究、证据和创作阶段；`ScenarioPackService` 在真实质量门禁前后报告交付检查。普通 fake Graph 不报告就没有阶段，失败研究不发 `research_completed` 成功事件，混合波次按研究/创作各自任务 ID 与成功 outcome 计算计数。Harness 仅绑定、让可信 phase 覆盖 payload 同名键，并按单 Run 去重；并发隔离回归通过。

初版把窄端口置于 runtime 导致现有层级矩阵拒绝三个低层依赖，AST RED 为 `1 failed, 11 passed, 47 subtests`。经协调方批准最小扩展后，ContextVar/安全类型下沉到 `core/operation_progress.py`，`runtime/operation_progress.py` 仅薄重导出；未修改依赖矩阵，AST 恢复为 `12 passed, 47 subtests passed in 2.20s`。

交付版本现在按“顶层版本 → 集合版本 → 严格 V1 默认”分派，所有路径都经过 Pydantic 重校验和序列化；嵌套非法 V2 action、内部 extra 不再旁路。EventHub 递归拒绝 `model_candidates`、`tool_args`、`raw_page`、`hidden_reasoning`，错误不回显值，普通业务正文中的 token 单词仍放行。`completed`/`target` 无论有无 phase 都独立拒绝 bool、负数和完成数大于目标数，`phase_started` 缺 phase 失败关闭。

R2 RED 证据包括：伪阶段 `4 failed, 3 passed`；真实信号窄端口缺失 `2 failed, 1 passed`；版本旁路 `3 failed, 5 deselected`；敏感键 `4 failed, 11 passed`；计数契约 `10 failed, 31 passed`；可信 phase/混合波次/失败研究语义 `3 failed`。最终聚焦组合为 `128 passed in 3.24s`，brief 指定子集为 `72 passed in 3.47s`。

Ruff check/format、8 个生产文件 mypy、compileall 均通过。最终全量 `.venv\Scripts\python.exe -m pytest -q`：退出码 0，`1693 passed, 9 skipped, 2 warnings, 280 subtests passed in 59.21s`。完整命令、实际信号来源、跳过规则及 13 文件 SHA256 见 [Task 5 R2 实施报告](sdd/agent-task-5-report.md)。无 Git、无提交；真实 Provider、网络、数据库、前端及生产 SSE 未验收。

### Task 5 R3 独立复核结论

- Task 5：complete。R3 补齐 camelCase/PascalCase 嵌套敏感键规范化，`modelCandidates`、`toolArgs`、`rawPage`、`hiddenReasoning` 与既有 snake/hyphen 形式均在发布前递归拒绝；普通业务正文中的 `token` 仍允许。
- 独立复核为 Approved（无 Critical/Important/Minor）；新鲜 EventHub/V2/契约回归 `67 passed`，阶段/SSE/契约回归 `83 passed`。R2 的真实节点进度、版本投影、计数与层级边界一并复核通过。
- R3 后完整回归稳定为 `1 failed, 1696 passed, 9 skipped, 2 warnings, 280 subtests`。唯一失败为未触及的 LiveAcceptance 测试：第二个 request 在授权绑定阶段按 `LIVE_AUTHORIZATION_REUSED` 正确拒绝，测试却断言后续 runner 已调用；经重复复现且与 EventHub 无导入/调用关系。本任务未修改无关实现或测试掩盖该矛盾，后续总门禁需单列处理。

## Task 6：安全、兼容和全量 Agent 门禁

Task 6 完成并经 R2 独立复核通过。V2 展示校验现在拒绝非 HTTPS、空/无 hostname、带凭据 URL，以及用户可见标题、导语、正文和动作标签中的明确主动内容/内部提示标记；不扫描内部 `intent_patch`，普通业务文本中的 `token` 仍可用。安全、质量、治理和 Task 1–5 联合回归通过；全量最新为 `1 failed, 1713 passed, 9 skipped, 2 warnings, 280 subtests`，唯一失败仍是已隔离的 LiveAcceptance 授权复用断言矛盾。

共享研究 fixture 已使用安全标题，但 R2 额外新增直接构造恶意来源标题的 `DeliveryPackV2` 渲染测试，确认输出已 HTML 转义且原始 `<script>` 不出现；独立复审通过（研究交付测试 `4 passed`）。账本中的冻结 V2 离线样例已通过模型校验，含 9 条热点、2 个 `example.test` 来源、一个 `rewrite_for_platform` 动作、provenance 和空 warnings。全仓格式检查仍有 98 个既有未格式化文件，`uv audit` 仍报告 `accelerate 1.14.0` 的 2 项无修复漏洞；均未掩盖或错误归入本任务。

### Task 5 R3：camelCase / PascalCase 嵌套敏感键补强（待独立复核）

R2 后续复核确认 `_SENSITIVE_KEYS` 已覆盖目标概念，但旧规范化仅处理小写和连字符；嵌套 `modelCandidates`、`toolArgs`、`rawPage`、`HiddenReasoning` 仍可绕过并进入事件历史。R3 严格测试先行，四个递归拒绝测试先得到 `4 failed, 15 deselected in 0.84s`，再以统一键名规范化做最小修复：保留连字符兼容，并将 camelCase/PascalCase 边界规范化为 snake_case 后匹配既有敏感键。它只进行结构键检查；普通业务正文含 `token` 继续允许，错误不回显敏感值。

R3 聚焦结果：`tests/runtime/test_event_hub.py` 为 `19 passed in 0.39s`；与 Task 5 API/契约组合为 `67 passed in 0.60s`。Ruff check、Ruff format、定向 mypy（`--follow-imports=skip`）、compileall 和架构守卫均通过，架构守卫为 `12 passed, 47 subtests passed in 2.53s`。跟随全部导入的 mypy 仍受未改动 `feedparser` 缺失类型桩影响，未将该既有问题误称为 R3 类型失败。

当前项目解释器的全量命令 `.venv\Scripts\python.exe -m pytest -q` 为 `1 failed, 1696 passed, 9 skipped, 2 warnings, 280 subtests passed in 82.26s`。唯一失败稳定复现两次，最小文件也稳定为 `1 failed, 8 passed`：`test_authorization_cannot_be_reused_with_a_new_request_id` 捕获 `LIVE_AUTHORIZATION_REUSED` 后仍断言第二个 `ordinary_chat` 已执行。其调用链 `LiveAcceptanceService.execute` → `_execute_owned` → `InMemoryLiveAcceptanceBudgetBinder.bind` 表明不同 `request_id` 在绑定阶段立即拒绝，按现有“先绑定后执行”语义不应调用 runner。R3 两个文件不依赖该路径且修改时间晚于失败文件，故不修改无关测试/实现掩盖该稳定既有断言冲突。

Task 5 状态保持 `DONE_WITH_CONCERNS`、**待独立复核**。R3 文件 SHA256：`runtime/event_hub.py` 为 `A0368E3EE20B12A2D4404945DE2D3293D8174DED8F5C3C5E5EA716038B7A03F6`，`tests/runtime/test_event_hub.py` 为 `D904E243C7523B6E77664F1AB17B751E202730BBB3C6A89E178EF2F74FE23343`。完整证据见 [Task 5 实施报告](sdd/agent-task-5-report.md)；永久 non-Git，无提交。

## Task 6：安全、兼容和全量 Agent 门禁

### 冻结 DeliverableSetV2 离线样例

以下样例仅用于前端契约联调和离线回归，所有条目均为虚构的契约占位内容，不代表真实新闻、实时热点或用户数据。样例的来源固定为 `example.test`，不含真实令牌、身份或外部访问凭据；治理测试会以 `DeliverableSetV2.model_validate()` 解析本段 JSON。

<!-- frozen-deliverable-set-v2-sample -->
```json
{
  "contract_version": "deliverable-set/2",
  "run_id": "frozen-operation-sample-20260917",
  "intent_revision": 1,
  "summary": {
    "message": "已生成 9 条离线契约样例热点，供前端验证排序、来源与动作展示。",
    "result_count": 9,
    "complete": true
  },
  "deliverables": [
    {
      "contract_version": "deliverable/2",
      "deliverable_id": "frozen-ranked-digest",
      "deliverable_kind": "ranked_digest",
      "platform": "research",
      "title": "离线运营热点样例",
      "lead": "以下内容只验证结构化交付契约，不构成真实新闻或事实陈述。",
      "citations": [
        {
          "citation_id": "source-a",
          "url": "https://example.test/source-a",
          "title": "离线示例来源 A",
          "source": "example.test",
          "published_at": "2026-09-17T08:00:00Z",
          "source_type": "public_page",
          "source_tier": "secondary",
          "verification_status": "unverified",
          "supports_item_ids": ["sample-1", "sample-2", "sample-3", "sample-4", "sample-5"],
          "independent_source_group": "example.test"
        },
        {
          "citation_id": "source-b",
          "url": "https://example.test/source-b",
          "title": "离线示例来源 B",
          "source": "example.test",
          "published_at": "2026-09-17T09:00:00Z",
          "source_type": "public_page",
          "source_tier": "secondary",
          "verification_status": "unverified",
          "supports_item_ids": ["sample-6", "sample-7", "sample-8", "sample-9"],
          "independent_source_group": "example.test"
        }
      ],
      "copy_text": "由运行时根据下列离线样例条目确定性生成。",
      "warnings": [],
      "content": {
        "kind": "ranked_digest",
        "selection_summary": "9 条虚构条目按固定重要性顺序排列，仅用于测试。",
        "ranking_basis": "importance",
        "items": [
          {"item_id": "sample-1", "rank": 1, "title": "离线样例热点 1", "occurred_at": "2026-09-17T08:00:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证第一名排序和来源映射。", "content_angles": ["样例解读"], "metrics": ["样例序号=1"], "source_refs": ["source-a"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-2", "rank": 2, "title": "离线样例热点 2", "occurred_at": "2026-09-17T08:01:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证连续排名和来源映射。", "content_angles": ["样例解读"], "metrics": ["样例序号=2"], "source_refs": ["source-a"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-3", "rank": 3, "title": "离线样例热点 3", "occurred_at": "2026-09-17T08:02:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证第三名展示。", "content_angles": ["样例解读"], "metrics": ["样例序号=3"], "source_refs": ["source-a"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-4", "rank": 4, "title": "离线样例热点 4", "occurred_at": "2026-09-17T08:03:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证中间排名展示。", "content_angles": ["样例解读"], "metrics": ["样例序号=4"], "source_refs": ["source-a"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-5", "rank": 5, "title": "离线样例热点 5", "occurred_at": "2026-09-17T08:04:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证动作可定位到具体条目。", "content_angles": ["样例解读"], "metrics": ["样例序号=5"], "source_refs": ["source-a"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-6", "rank": 6, "title": "离线样例热点 6", "occurred_at": "2026-09-17T09:00:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证第二来源的关联。", "content_angles": ["样例解读"], "metrics": ["样例序号=6"], "source_refs": ["source-b"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-7", "rank": 7, "title": "离线样例热点 7", "occurred_at": "2026-09-17T09:01:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证长列表滚动展示。", "content_angles": ["样例解读"], "metrics": ["样例序号=7"], "source_refs": ["source-b"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-8", "rank": 8, "title": "离线样例热点 8", "occurred_at": "2026-09-17T09:02:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证末尾前一项展示。", "content_angles": ["样例解读"], "metrics": ["样例序号=8"], "source_refs": ["source-b"], "confidence": "low", "verification_status": "unverified"},
          {"item_id": "sample-9", "rank": 9, "title": "离线样例热点 9", "occurred_at": "2026-09-17T09:03:00Z", "summary": "离线契约样例；不代表真实新闻。", "why_it_matters": "验证第九项和完整计数。", "content_angles": ["样例解读"], "metrics": ["样例序号=9"], "source_refs": ["source-b"], "confidence": "low", "verification_status": "unverified"}
        ]
      }
    }
  ],
  "next_actions": [
    {
      "action_id": "rewrite-sample-1",
      "action_type": "rewrite_for_platform",
      "label": "将第 1 条改写为小红书版本",
      "target_deliverable_id": "frozen-ranked-digest",
      "target_item_ids": ["sample-1"],
      "intent_patch": {"platform": "xiaohongshu", "target_item_ids": ["sample-1"]},
      "requires_user_input": true
    }
  ],
  "provenance": {
    "source_count": 2,
    "verified_source_count": 0,
    "candidate_count": 9,
    "merged_event_count": 9,
    "retained_count": 9,
    "eliminated_count": 0,
    "collection_window_start": "2026-09-16T00:00:00Z",
    "collection_window_end": "2026-09-17T00:00:00Z",
    "ranking_basis": "importance"
  },
  "degraded": false,
  "warnings": []
}
```

### Task 6：安全、兼容和全量门禁实施结果

状态：`DONE_WITH_CONCERNS`，待独立复核。完整过程、命令、SHA256 与未验证边界见 [Task 6 实施报告](sdd/agent-task-6-report.md)。永久 non-Git，无提交。

- `DeliveryPresentationValidator` 对 V2 引用执行 HTTPS、hostname、无凭据与无空白失败关闭；对渲染后的 copy_text 及所有公开展示文本递归拒绝 `<script`、`javascript:`、`system prompt`、`hidden reasoning`。下一轮 `intent_patch`、运行标识、计数和引用关系标识不属于展示字段，不读取也不改写；普通业务文本中的 `token` 被明确允许。
- 新增独立安全测试，覆盖恶意 URL、内部提示标记、跨引用、Markdown、动作标签、HTTPS 大小写边界与内部 intent patch 保持性。安全 RED 为 `10 failed, 2 passed`，实现后安全/质量/账本治理聚焦 GREEN 为 `60 passed`。
- 上方 JSON 已由治理测试中的 `DeliverableSetV2.model_validate()` 解析：1 个排名交付物包含 9 条明确标为虚构的离线样例，2 个 `example.test` 来源，1 个 `rewrite_for_platform` 动作、完整 provenance 与空 warnings；不含真实新闻、令牌或用户数据。
- 安全门首次使 Task 4 的共享 fixture 中 `<script>` 来源标题正确失败关闭。经批准的最小测试范围扩展仅把该虚构标题改为安全文本，并同步一条字面断言；恶意 `<script>` 的独立安全拒绝测试保留。

门禁结果：Task 1–5 关联回归 `226 passed`；共享 fixture/Research V2/安全回归 `35 passed`；AST 架构守卫 `12 passed, 47 subtests passed`；compileall、全库 Ruff check、`mypy src`（291 文件）均通过。本任务五个 Python 文件格式检查通过；全库 Ruff format check 仍有 `98 files would be reformatted` 的历史格式欠账，未越界批量改写。`uv audit --frozen` 报告 `accelerate 1.14.0` 两条无可用修复版本的已知漏洞。

最终全量 `.venv\Scripts\python.exe -m pytest -q` 为 `1 failed, 1712 passed, 9 skipped, 2 warnings, 280 subtests passed in 56.08s`。唯一失败稳定最小复现为 `tests/unit/harness/test_live_acceptance.py` 的 `1 failed, 8 passed`：第二 request 已在 `InMemoryLiveAcceptanceBudgetBinder.bind` 按 `LIVE_AUTHORIZATION_REUSED` 正确拒绝，测试却断言后续 runner 已执行 `ordinary_chat`。Task 6 未修改该 Harness 调用链且质量模块无导入/调用关系，未用无关改动掩盖该断言冲突。两条 warning 均为既有 Starlette/Polars 依赖提示。

### Task 6 R2：研究 Markdown 恶意来源标题回归补强（待独立复核）

复核发现共享研究 fixture 改为安全标题后，原 Markdown 测试不再直接覆盖恶意来源标题。
仅在 `tests/unit/capabilities/research_v2/test_delivery.py` 新增独立构造的
`DeliveryPackV2` 恶意来源标题样例，既不复用共享 fixture，也不改生产渲染器。首个
精确输出断言按 TDD 得到 RED：渲染器在 HTML 转义外还转义 Markdown 圆括号；随后收敛为
安全语义断言，要求输出同时含 `&lt;script&gt;` 和 `&lt;/script&gt;`，且不含原始
`<script>`。GREEN：Research delivery `4 passed`；Task 1–5 与 Task 6 关联回归
`291 passed in 6.48s`；AST `12 passed, 47 subtests passed`；compileall、全库 Ruff
check 与该文件格式检查均通过。

最新全量为 `1 failed, 1713 passed, 9 skipped, 2 warnings, 280 subtests passed in
52.31s`。唯一失败仍为未触及的 LiveAcceptance 授权复用断言冲突：第二 request 已在
`InMemoryLiveAcceptanceBudgetBinder.bind` 正确以 `LIVE_AUTHORIZATION_REUSED` 拒绝，
测试却期待 runner 已执行。未修改无关 Harness 路径掩盖该既有问题。Task 6 保持
`DONE_WITH_CONCERNS`，待独立复核；R2 测试文件 SHA256：
`9A795AAA00AC139ECC3111FD1E0B07B1D87605830A0BE67FC6C4A7CF6A2FBAE9`。

## 2026-09-17：本地真实组合根 V1/V2 契约兼容修复

### 根因与最小修复

- 已确认本地真实组合根默认创建 `ResearchPipelineSettings()`，当前为 V1；Gate 开启时装配 `FreePublicResearchProvider`，但此前仍把默认 `deliverable-set/2` 传给 `OperationAgent`。
- `OperationAgent` 的 V2 fail-close 会在 Supervisor 和 research provider 之前返回 `RESEARCH_UNAVAILABLE`，所以原故障不是来源不可用，而是组合根把 V1 研究管线与 V2 交付契约错误配对。
- 最小修复只调整 `harness/local_real_factory.py` 的默认选择：调用方未显式指定时，实际装配 `ResearchV2ProviderAdapter` 才选择 `deliverable-set/2`；其他 V1/免费公开源装配显式选择 `deliverable-set/1`。未删除或放宽 `OperationAgent` 的 V2 fail-close，未修改 UI、API、来源实现、来源准入或付费边界。

### TDD 证据

新增真实组合根到 Conversation、Run、SSE 的回归断言，要求 Web Search Gate 开启且 V2 管线关闭时，研究 provider 被调用一次，并出现 `research_started`。

RED：

```powershell
.venv\Scripts\python.exe -m pytest -q tests\unit\harness\test_local_real_factory.py::test_factory_v1_pipeline_reaches_research_provider_with_default_contract
```

修复前结果：退出码 `1`；断言 `research.calls == 1` 实际为 `0`，证实请求在 provider 之前失败。

GREEN：同一命令修复后退出码 `0`，`1 passed in 2.19s`。

### 回归与静态门禁

- V1/V2、Agent 集成、Intent V2、阶段事件和 SSE 关联回归：`130 passed in 4.34s`。
- 架构守卫：`47 passed, 124 subtests passed in 4.83s`；`compileall -q src` 退出码 `0`。
- 全库 Ruff lint：`All checks passed!`；两个实际改动 Python 文件格式检查：`2 files already formatted`。
- `mypy src`：`Success: no issues found in 291 source files`。
- 全量 pytest：`1 failed, 1713 passed, 9 skipped, 2 warnings, 280 subtests passed in 49.32s`。唯一失败仍是既有 `test_authorization_cannot_be_reused_with_a_new_request_id` 断言矛盾；本次未修改该路径掩盖失败。

### 重启与真实重放

- 重启前确认 8080 为 `python -m efficiency_platform_agent --env-file D:\软件目录\efficiency-platform\efficiency-platform-agent\.env --host 127.0.0.1 --port 8080`，旧监听 PID `17728`；`/health/live` 返回 `status=ok`。
- 只停止已复核的旧 PID，并从 Agent 项目目录用项目 `.venv` 隐藏启动；新 8080 监听 PID 为 `58848`，`/health/live` 返回 `status=ok, mode=s2_fake_in_memory`。
- UI 5190 重启前后均为 PID `79744`，命令仍是 `ai-efficiency-platform-ui` 的 Vite；未停止或重启 UI。
- 真实热点请求 Run：`run-d197e03015ae44c683a49096c131025b`，终态 `succeeded`，交付契约 `deliverable-set/1`。
- SSE 共 13 个事件，关键顺序为 `phase_started(understanding_request)` → `phase_started(collecting_sources)` → `research_started` → `phase_started(checking_evidence)` → `research_completed` → `phase_started(checking_delivery)` → `quality_checked` → `deliverable` → `stream_done`；事件中无 `RESEARCH_UNAVAILABLE`。
- 来源层返回 9 个 Hacker News 链接，但成品明确说明仅约 4 条可直接归为 AI 行业动态，其余为标题无法核验、邻接领域或非 AI 内容；没有把“9 条 AI 热点”目标伪装为满足，也没有补造来源。

### 未验证边界

- 本次证明 V1 本地真实装配可以进入 provider 和完整 Run/SSE 链；未把 V2 生产开关、PostgreSQL 持久化、共享 DDL、Java/UI 联调或生产部署描述为已验收。
- 未逐页独立核验 9 个来源正文、精确发布时间和事实内容；真实输出自身已披露标题级核验与时间窗不一致限制。
- 未启用付费或非公开来源，未修改免费来源准入与失败策略。

## 2026-09-17：本地真实组合根 V1/V2 契约选择防回归补测

### 范围与结果

- 仅修改 `tests/unit/harness/test_local_real_factory.py`、`tests/unit/harness/test_research_v2_factory.py` 并追加本账本；`src/efficiency_platform_agent/harness/local_real_factory.py` 的最终 SHA256 仍为 `EB684369946710663E1A4E78109B5B0E7A21F84D3220E4299129839537279CD3`，未改变已正确的生产选择。
- V1/免费公开源测试直接截获组合根传给 `build_operation_agent()` 的参数：未显式指定时必须为 `deliverable-set/1`，显式 `deliverable-set/2` 不得被默认值覆盖。
- Research V2 adapter 测试直接截获同一参数：未显式指定时必须为 `deliverable-set/2`，显式 `deliverable-set/1` 不得被默认值覆盖。

### TDD 变异证据

- 临时模拟旧的固定 V2 映射后，聚焦测试为 `1 failed, 3 passed`；V1/free 默认断言精确报告实际 `deliverable-set/2`、期望 `deliverable-set/1`。
- 临时模拟缺失 V2 分支的固定 V1 映射后，聚焦测试为 `1 failed, 3 passed`；Research V2 adapter 默认断言精确报告实际 `deliverable-set/1`、期望 `deliverable-set/2`。
- 恢复当前生产实现后，同一四个参数化聚焦用例为 `4 passed in 2.24s`；两种显式版本用例在两次变异中均保持通过，证明显式值优先级被单独锁定。

### 新鲜验证

- 两份 Harness 单元测试与全量 AST 架构守卫：`77 passed, 124 subtests passed in 6.15s`。
- 两份改动测试文件 Ruff lint：`All checks passed!`；Ruff format check：`2 files already formatted`。
- `compileall -q src` 与两份测试文件 `py_compile` 均退出码 `0`。
- 未执行 Git 操作，未重启或改动现有服务。
